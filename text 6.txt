-- ================================================
-- 🔥 AIMBOT PRO v13.2 - SAFE EDITION
-- ✅ GUI Size Adjustment (Scale Slider)
-- ✅ Removed Particle Effects (clean look)
-- ✅ Loading Screen
-- ✅ All Other Features Preserved
-- ================================================

-- ========== SERVICES ==========
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")
local Stats = game:GetService("Stats")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

local function SafeSet(prop, value)
    pcall(function() Lighting[prop] = value end)
end

-- ========== LOADING SCREEN ==========
local LoadingGui = Instance.new("ScreenGui")
LoadingGui.Name = "AimbotLoading"
LoadingGui.IgnoreGuiInset = true
LoadingGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
LoadingGui.Parent = CoreGui

local LoadBG = Instance.new("Frame")
LoadBG.Size = UDim2.new(1, 0, 1, 0)
LoadBG.BackgroundColor3 = Color3.fromRGB(8, 4, 18)
LoadBG.BorderSizePixel = 0
LoadBG.ZIndex = 9999
LoadBG.Parent = LoadingGui

local LoadGrad = Instance.new("UIGradient", LoadBG)
LoadGrad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 8, 45)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(8, 4, 18)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(15, 6, 35))
}
LoadGrad.Rotation = 135

-- Loading Title
local LoadTitle = Instance.new("TextLabel")
LoadTitle.Size = UDim2.new(0, 600, 0, 60)
LoadTitle.Position = UDim2.new(0.5, -300, 0.35, -30)
LoadTitle.BackgroundTransparency = 1
LoadTitle.Text = "✦ AIMBOT PRO"
LoadTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
LoadTitle.TextSize = 48
LoadTitle.Font = Enum.Font.GothamBold
LoadTitle.ZIndex = 10000
LoadTitle.Parent = LoadBG

local LoadSub = Instance.new("TextLabel")
LoadSub.Size = UDim2.new(0, 400, 0, 24)
LoadSub.Position = UDim2.new(0.5, -200, 0.35, 30)
LoadSub.BackgroundTransparency = 1
LoadSub.Text = "v13.2 - SAFE EDITION"
LoadSub.TextColor3 = Color3.fromRGB(180, 100, 255)
LoadSub.TextSize = 16
LoadSub.Font = Enum.Font.GothamBold
LoadSub.ZIndex = 10000
LoadSub.Parent = LoadBG

-- Loading Bar BG
local LoadBarBG = Instance.new("Frame")
LoadBarBG.Size = UDim2.new(0, 400, 0, 12)
LoadBarBG.Position = UDim2.new(0.5, -200, 0.5, 0)
LoadBarBG.BackgroundColor3 = Color3.fromRGB(30, 15, 55)
LoadBarBG.BorderSizePixel = 0
LoadBarBG.ZIndex = 10000
LoadBarBG.Parent = LoadBG
Instance.new("UICorner", LoadBarBG).CornerRadius = UDim.new(1, 0)

local LoadBarFill = Instance.new("Frame")
LoadBarFill.Size = UDim2.new(0, 0, 1, 0)
LoadBarFill.BackgroundColor3 = Color3.fromRGB(180, 100, 255)
LoadBarFill.BorderSizePixel = 0
LoadBarFill.ZIndex = 10001
LoadBarFill.Parent = LoadBarBG
Instance.new("UICorner", LoadBarFill).CornerRadius = UDim.new(1, 0)
local LoadBarGrad = Instance.new("UIGradient", LoadBarFill)
LoadBarGrad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 100, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 80, 255))
}

local LoadPercent = Instance.new("TextLabel")
LoadPercent.Size = UDim2.new(0, 200, 0, 24)
LoadPercent.Position = UDim2.new(0.5, -100, 0.5, 25)
LoadPercent.BackgroundTransparency = 1
LoadPercent.Text = "0%"
LoadPercent.TextColor3 = Color3.fromRGB(220, 180, 255)
LoadPercent.TextSize = 14
LoadPercent.Font = Enum.Font.GothamBold
LoadPercent.ZIndex = 10000
LoadPercent.Parent = LoadBG

local LoadStatus = Instance.new("TextLabel")
LoadStatus.Size = UDim2.new(0, 400, 0, 20)
LoadStatus.Position = UDim2.new(0.5, -200, 0.5, 50)
LoadStatus.BackgroundTransparency = 1
LoadStatus.Text = "Initializing..."
LoadStatus.TextColor3 = Color3.fromRGB(150, 150, 180)
LoadStatus.TextSize = 12
LoadStatus.Font = Enum.Font.Gotham
LoadStatus.ZIndex = 10000
LoadStatus.Parent = LoadBG

-- Loading function
local function UpdateLoading(percent, status)
    TweenService:Create(LoadBarFill, TweenInfo.new(0.3, Enum.EasingStyle.Quart), {
        Size = UDim2.new(percent/100, 0, 1, 0)
    }):Play()
    LoadPercent.Text = math.floor(percent) .. "%"
    LoadStatus.Text = status
    task.wait(0.1)
end

-- Run loading sequence
task.spawn(function()
    UpdateLoading(10, "Loading services...")
    task.wait(0.2)
    UpdateLoading(25, "Initializing settings...")
    task.wait(0.2)
    UpdateLoading(40, "Setting up crosshair system...")
    task.wait(0.2)
    UpdateLoading(55, "Building ESP system...")
    task.wait(0.2)
    UpdateLoading(70, "Loading movement hacks...")
    task.wait(0.2)
    UpdateLoading(85, "Building GUI interface...")
    task.wait(0.2)
    UpdateLoading(100, "Complete!")
    task.wait(0.5)
    
    -- Fade out
    TweenService:Create(LoadBG, TweenInfo.new(0.6, Enum.EasingStyle.Quart), {BackgroundTransparency = 1}):Play()
    for _, c in ipairs(LoadBG:GetChildren()) do
        if c:IsA("TextLabel") or c:IsA("Frame") then
            TweenService:Create(c, TweenInfo.new(0.6, Enum.EasingStyle.Quart), {TextTransparency = 1}):Play()
        end
    end
    task.wait(0.7)
    LoadingGui:Destroy()
end)

-- ========== SETTINGS ==========
local Settings = {
    -- GUI
    GUIScale = 100,

    -- Aimbot
    AimbotEnabled = false,
    AimbotHitbox = "Head",
    AimbotMultipoint = false,
    AimbotMultipointScale = 50,
    FOV = 150,
    Smoothness = 5,
    ShowFOV = true,
    FOVThickness = 2,
    TeamCheck = false,
    WallCheck = false,
    AimbotMode = "Hold",
    BaimEnabled = false,
    BaimAfterMisses = 0,
    BaimInAir = false,

    -- Crosshair
    CrosshairStyle = "Heart",
    CrosshairSize = 100,
    CrosshairThickness = 100,
    CrosshairOpacity = 100,
    CrosshairRotation = true,
    CrosshairColor = {255, 80, 180},

    -- ESP
    ESPEnabled = false,
    ESPBoxStyle = "2D",
    ESPBoxes = true,
    ESPNames = true,
    ESPHealth = true,
    ESPDistance = false,
    ESPWeapon = false,
    ESPSkeleton = false,
    ESPSnapLines = false,
    ESPTracers = false,
    ESPDormant = false,
    ESPSize = 100,
    ESPOpacity = 100,

    -- Chams
    ChamsEnabled = false,
    ChamsMode = "Visible",
    GlowEnabled = false,

    -- Radar
    RadarEnabled = false,
    RadarSize = 180,
    RadarZoom = 50,

    -- World
    FullBright = false,
    NightMode = false,
    RemoveVisualRecoil = false,
    ThirdPerson = false,
    ThirdPersonZoom = 15,
    CameraFOV = 70,

    -- Anti-Aim
    AntiAimEnabled = false,
    YawMode = "Spin",
    YawOffset = 0,
    YawJitterRange = 45,
    PitchMode = "None",
    SpinSpeed = 5,
    RollAngle = 0,
    ManualAA = "None",
    InverterActive = false,
    Freestanding = false,

    -- Weather
    WeatherEnabled = false,
    WeatherType = "None",
    WeatherIntensity = 50,

    -- Toggle Button
    ToggleButtonLocked = false,

    -- MOVEMENT
    WallJump = false,
    BunnyHop = false,
    InfinityJump = false,
    JumpBug = false,
    CircleStrafe = false,
    StrafeInAir = false,
    QuickStop = false,
    WalkSpeedBoost = false,
    WalkSpeedValue = 16,
    JumpPowerBoost = false,
    JumpPowerValue = 50,
    BypassMovement = false,

    -- EXTRA
    DisableConsole = false,
    DisableServerAds = false,
    ShowFPS = false,
    FakePing = false,
    FakePingValue = 999,
    ShowMS = false,
    ShowPlayerFPS = false,
    ShowPlayerMS = false,
    AutoPeek = false,

    -- INVENTORY
    ToolESP = false,
    ToolESPName = false,
    ToolESPColor = {50, 255, 50},
    ToolESPNameColor = {255, 255, 255},

    -- INDICATOR
    TeleportCooldownInd = false,
    InventoryFullWarning = false,
    Compass = false,
    NearbyLoot = false,
    DamageText = false,
    DamageTextColor = {255, 50, 50},
    NearbyEnemy = false,
    NearbyEnemyColor = {255, 50, 50},

    -- Colors
    FOVColor = {180, 100, 255},
    ESPColor = {180, 100, 255},
    ChamsColor = {180, 50, 255},
    ChamsOutlineColor = {255, 255, 255},
    GlowColor = {180, 100, 255},
    AccentColor = {180, 100, 255},
    SkeletonColor = {255, 255, 255},
    WorldSkyColor = {107, 127, 255},
    WorldAmbient = {70, 70, 70},
    WorldFog = false,
    WorldFogColor = {200, 200, 200},
    WorldFogStart = 0,
    WorldFogEnd = 1000,
    WorldBrightness = 1,
}

local function C3(t) return Color3.fromRGB(t[1], t[2], t[3]) end

local AimbotActive = false
local ToggleActive = false
local MissCount = 0

-- ========== DRAWING / CROSSHAIR ==========
local HeartDots = {}
local SquareLines = {}
local HexagonLines = {}
local StarLines = {}
local FOVCircle

for t = 0, 2*math.pi, 0.2 do
    local s, d = pcall(Drawing.new, "Circle")
    if s and d then
        d.Radius = 1.6; d.Color = C3(Settings.CrosshairColor)
        d.Filled = true; d.Transparency = 1; d.Visible = false
        table.insert(HeartDots, d)
    end
end

for i = 1, 5 do
    local s, l = pcall(Drawing.new, "Line")
    if s and l then
        l.Color = C3(Settings.CrosshairColor); l.Thickness = 1.5
        l.Transparency = 1; l.Visible = false
        table.insert(SquareLines, l)
    end
end

for i = 1, 7 do
    local s, l = pcall(Drawing.new, "Line")
    if s and l then
        l.Color = C3(Settings.CrosshairColor); l.Thickness = 1.5
        l.Transparency = 1; l.Visible = false
        table.insert(HexagonLines, l)
    end
end

for i = 1, 11 do
    local s, l = pcall(Drawing.new, "Line")
    if s and l then
        l.Color = C3(Settings.CrosshairColor); l.Thickness = 1.5
        l.Transparency = 1; l.Visible = false
        table.insert(StarLines, l)
    end
end

local sok, c = pcall(Drawing.new, "Circle")
if sok and c then
    c.Visible = false; c.Radius = Settings.FOV
    c.Color = C3(Settings.FOVColor); c.Thickness = 2
    c.Transparency = 1; c.Filled = false
    FOVCircle = c
end

local function RotateV2(v, angle)
    return Vector2.new(math.cos(angle)*v.X - math.sin(angle)*v.Y, math.sin(angle)*v.X + math.cos(angle)*v.Y)
end

-- ========== ESP SYSTEM ==========
local ESPData = {}

local function NewLine()
    local s, l = pcall(Drawing.new, "Line")
    if s and l then
        l.Color = C3(Settings.ESPColor); l.Thickness = 1.5
        l.Transparency = 1; l.Visible = false
        return l
    end
end

local function NewText(size)
    local s, t = pcall(Drawing.new, "Text")
    if s and t then
        t.Color = C3(Settings.ESPColor); t.Size = size or 13
        t.Font = Drawing.Fonts.UI; t.Outline = true
        t.OutlineColor = Color3.new(0,0,0); t.Visible = false; t.Center = true
        return t
    end
end

local function CreateESP(player)
    if ESPData[player] then return end
    local esp = {BoxLines = {}, CornerLines = {}, Box3DLines = {}, SkeletonLines = {}}
    for i = 1, 4 do table.insert(esp.BoxLines, NewLine()) end
    for i = 1, 8 do table.insert(esp.CornerLines, NewLine()) end
    for i = 1, 12 do table.insert(esp.Box3DLines, NewLine()) end
    for i = 1, 6 do table.insert(esp.SkeletonLines, NewLine()) end
    esp.NameText = NewText(13)
    esp.DistText = NewText(11)
    esp.WeaponText = NewText(11)
    esp.HpBG = NewLine()
    if esp.HpBG then esp.HpBG.Color = Color3.new(0,0,0); esp.HpBG.Thickness = 5; esp.HpBG.Transparency = 0.6 end
    esp.HpFill = NewLine()
    if esp.HpFill then esp.HpFill.Thickness = 3 end
    esp.Tracer = NewLine()
    esp.SnapLine = NewLine()
    esp.DormantMarker = NewLine()
    ESPData[player] = esp
end

local function HideESP(esp)
    if not esp then return end
    for _, group in pairs({esp.BoxLines, esp.CornerLines, esp.Box3DLines, esp.SkeletonLines}) do
        for _, l in ipairs(group) do if l then l.Visible = false end end
    end
    for _, k in ipairs({"NameText","DistText","WeaponText","HpBG","HpFill","Tracer","SnapLine","DormantMarker"}) do
        if esp[k] then esp[k].Visible = false end
    end
end

-- ========== CHAMS ==========
local ChamsObjects = {}
local GlowObjects = {}

local function ApplyChams(player)
    if not player.Character then return end
    if ChamsObjects[player] then pcall(function() ChamsObjects[player]:Destroy() end) end
    local s, h = pcall(function()
        local hl = Instance.new("Highlight")
        hl.FillColor = C3(Settings.ChamsColor)
        hl.OutlineColor = C3(Settings.ChamsOutlineColor)
        hl.FillTransparency = 0.45
        hl.OutlineTransparency = 0
        if Settings.ChamsMode == "Visible" then hl.DepthMode = Enum.HighlightDepthMode.Occluded
        elseif Settings.ChamsMode == "Invisible" then hl.DepthMode = Enum.HighlightDepthMode.Occluded; hl.FillTransparency = 1
        elseif Settings.ChamsMode == "Wall" then hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop end
        hl.Adornee = player.Character
        hl.Parent = player.Character
        return hl
    end)
    if s and h then ChamsObjects[player] = h end
end

local function RemoveChams(player)
    if ChamsObjects[player] then pcall(function() ChamsObjects[player]:Destroy() end); ChamsObjects[player] = nil end
end

local function ApplyGlow(player)
    if GlowObjects[player] then pcall(function() GlowObjects[player]:Destroy() end); GlowObjects[player] = nil end
    if not player.Character then return end
    local root = player.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    local s, g = pcall(function()
        local light = Instance.new("PointLight")
        light.Color = C3(Settings.GlowColor)
        light.Brightness = 3; light.Range = 15
        light.Parent = root
        return light
    end)
    if s and g then GlowObjects[player] = g end
end

local function RemoveGlow(player)
    if GlowObjects[player] then pcall(function() GlowObjects[player]:Destroy() end); GlowObjects[player] = nil end
end

local function RefreshChams()
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            RemoveChams(p); RemoveGlow(p)
            if Settings.ChamsEnabled then ApplyChams(p) end
            if Settings.GlowEnabled then ApplyGlow(p) end
        end
    end
end

local function UpdateChamsColor()
    for _, h in pairs(ChamsObjects) do
        pcall(function() h.FillColor = C3(Settings.ChamsColor); h.OutlineColor = C3(Settings.ChamsOutlineColor) end)
    end
    for _, g in pairs(GlowObjects) do pcall(function() g.Color = C3(Settings.GlowColor) end) end
end

-- ========== WALL CHECK / HITBOX ==========
local function CanSee(part)
    if not Settings.WallCheck then return true end
    local origin = Camera.CFrame.Position
    local dir = (part.Position - origin).Unit * 1000
    local ray = Ray.new(origin, dir)
    local ignore = {workspace.Terrain, LocalPlayer.Character}
    local hit = workspace:FindPartOnRayWithIgnoreList(ray, ignore)
    if hit then
        local ch = part:FindFirstAncestorWhichIsA("Model")
        if ch and ch:IsAncestorOf(hit) then return true end
        return false
    end
    return true
end

local HitboxMap = {
    Head = "Head", Chest = "UpperTorso", Stomach = "LowerTorso",
    Pelvis = "HumanoidRootPart", Body = "HumanoidRootPart"
}

local function GetHitboxPart(character, hitboxName)
    local pn = HitboxMap[hitboxName] or "Head"
    return character:FindFirstChild(pn) or character:FindFirstChild("Torso") or character:FindFirstChild("UpperTorso") or character:FindFirstChild("HumanoidRootPart")
end

local function GetClosestPlayer()
    local closest, closestDist, closestPos = nil, Settings.FOV, nil
    local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)
    local useHitbox = Settings.AimbotHitbox
    if Settings.BaimEnabled then
        local ch = LocalPlayer.Character
        local hu = ch and ch:FindFirstChildOfClass("Humanoid")
        local air = hu and (hu:GetState() == Enum.HumanoidStateType.Freefall or hu:GetState() == Enum.HumanoidStateType.Jumping)
        if Settings.BaimInAir and air then useHitbox = "Chest" end
        if Settings.BaimAfterMisses > 0 and MissCount >= Settings.BaimAfterMisses then useHitbox = "Chest" end
    end
    for _, player in ipairs(Players:GetPlayers()) do
        if player == LocalPlayer then continue end
        if Settings.TeamCheck and player.Team == LocalPlayer.Team then continue end
        local ch = player.Character
        if not ch then continue end
        local hu = ch:FindFirstChildOfClass("Humanoid")
        local pt = GetHitboxPart(ch, useHitbox)
        if hu and hu.Health > 0 and pt then
            local sp, os = Camera:WorldToViewportPoint(pt.Position)
            if os then
                local d = (Vector2.new(sp.X, sp.Y) - center).Magnitude
                if d < closestDist and CanSee(pt) then
                    closestDist = d; closest = player; closestPos = pt.Position
                end
            end
        end
    end
    return closest, closestPos
end

-- ========== WORLD ==========
local OriginalLighting = {
    Ambient = Lighting.Ambient, OutdoorAmbient = Lighting.OutdoorAmbient,
    Brightness = Lighting.Brightness, FogColor = Lighting.FogColor,
    FogStart = Lighting.FogStart, FogEnd = Lighting.FogEnd,
    ColorShift_Bottom = Lighting.ColorShift_Bottom, ColorShift_Top = Lighting.ColorShift_Top,
    ClockTime = Lighting.ClockTime, GlobalShadows = Lighting.GlobalShadows,
}
local OriginalFOV = Camera.FieldOfView

local function ApplyWorldSettings()
    if Settings.FullBright then
        SafeSet("Ambient", Color3.fromRGB(255, 255, 255))
        SafeSet("OutdoorAmbient", Color3.fromRGB(255, 255, 255))
        SafeSet("Brightness", 3); SafeSet("GlobalShadows", false)
    elseif Settings.NightMode then
        SafeSet("Ambient", Color3.fromRGB(0, 0, 40))
        SafeSet("OutdoorAmbient", Color3.fromRGB(0, 0, 40))
        SafeSet("Brightness", 0.3); SafeSet("ClockTime", 0)
    else
        SafeSet("Ambient", C3(Settings.WorldAmbient))
        SafeSet("OutdoorAmbient", C3(Settings.WorldAmbient))
        SafeSet("Brightness", Settings.WorldBrightness)
        SafeSet("ColorShift_Bottom", C3(Settings.WorldSkyColor))
        SafeSet("ColorShift_Top", C3(Settings.WorldSkyColor))
    end
    if Settings.WorldFog then
        SafeSet("FogColor", C3(Settings.WorldFogColor))
        SafeSet("FogStart", Settings.WorldFogStart)
        SafeSet("FogEnd", Settings.WorldFogEnd)
    else
        SafeSet("FogEnd", 100000); SafeSet("FogStart", 0)
    end
    Camera.FieldOfView = Settings.CameraFOV
    pcall(function()
        if Settings.ThirdPerson then
            LocalPlayer.CameraMinZoomDistance = Settings.ThirdPersonZoom
            LocalPlayer.CameraMaxZoomDistance = Settings.ThirdPersonZoom
        else
            LocalPlayer.CameraMinZoomDistance = 0.5
            LocalPlayer.CameraMaxZoomDistance = 128
        end
    end)
end

local function ResetWorld()
    for k, v in pairs(OriginalLighting) do pcall(function() Lighting[k] = v end) end
    Camera.FieldOfView = OriginalFOV
    pcall(function()
        LocalPlayer.CameraMinZoomDistance = 0.5
        LocalPlayer.CameraMaxZoomDistance = 128
    end)
end

-- ========== ANTI-AIM ==========
local spinAngle = 0
local jitterFlip = 1

local function GetClosestEnemyDir()
    local ch = LocalPlayer.Character
    if not ch then return nil end
    local hrp = ch:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    local cp, cd = nil, math.huge
    for _, p in ipairs(Players:GetPlayers()) do
        if p == LocalPlayer then continue end
        local c = p.Character
        if c and c:FindFirstChild("HumanoidRootPart") then
            local d = (c.HumanoidRootPart.Position - hrp.Position).Magnitude
            if d < cd then cd = d; cp = c end
        end
    end
    if cp then return (cp.HumanoidRootPart.Position - hrp.Position).Unit end
    return nil
end

local function ApplyAntiAim(dt)
    if not Settings.AntiAimEnabled then return end
    local ch = LocalPlayer.Character
    if not ch then return end
    local hrp = ch:FindFirstChild("HumanoidRootPart")
    local hu = ch:FindFirstChildOfClass("Humanoid")
    if not hrp or not hu then return end
    local yaw, pitch, roll = 0, 0, math.rad(Settings.RollAngle)
    if Settings.ManualAA ~= "None" then
        if Settings.ManualAA == "Left" then yaw = math.rad(90)
        elseif Settings.ManualAA == "Right" then yaw = math.rad(-90)
        elseif Settings.ManualAA == "Back" then yaw = math.rad(180)
        elseif Settings.ManualAA == "Front" then yaw = 0 end
    elseif Settings.Freestanding then
        local d = GetClosestEnemyDir()
        if d then yaw = math.atan2(-d.X, -d.Z) + math.rad(90) end
    else
        if Settings.YawMode == "Spin" then
            spinAngle = (spinAngle + dt * Settings.SpinSpeed * 60) % 360
            yaw = math.rad(spinAngle)
        elseif Settings.YawMode == "Backwards" then yaw = math.rad(180)
        elseif Settings.YawMode == "Jitter" then
            jitterFlip = -jitterFlip
            yaw = math.rad(Settings.YawJitterRange * jitterFlip)
        elseif Settings.YawMode == "Random" then yaw = math.rad(math.random(-180, 180))
        elseif Settings.YawMode == "Custom" then yaw = math.rad(Settings.YawOffset) end
    end
    yaw = yaw + math.rad(Settings.YawOffset)
    if Settings.InverterActive then yaw = yaw + math.rad(180) end
    if Settings.PitchMode == "Down" then pitch = math.rad(-89)
    elseif Settings.PitchMode == "Up" then pitch = math.rad(89)
    elseif Settings.PitchMode == "Zero" then pitch = 0
    elseif Settings.PitchMode == "Fake Down" then pitch = math.rad(-45) end
    pcall(function()
        hu.AutoRotate = false
        hrp.CFrame = CFrame.new(hrp.Position) * CFrame.Angles(pitch, yaw, roll)
    end)
end

-- ========== BYPASS MOVEMENT (SAFE) ==========
local function SafeBypassUpdate()
    if not Settings.BypassMovement then return end
    pcall(function()
        local ch = LocalPlayer.Character
        if not ch then return end
        local hu = ch:FindFirstChildOfClass("Humanoid")
        if not hu then return end
        if Settings.WalkSpeedBoost then
            if hu.WalkSpeed ~= Settings.WalkSpeedValue then
                hu.WalkSpeed = Settings.WalkSpeedValue
            end
        end
        if Settings.JumpPowerBoost then
            if hu.JumpPower ~= Settings.JumpPowerValue then
                hu.JumpPower = Settings.JumpPowerValue
            end
        end
    end)
end

-- ========== CHARACTER ==========
LocalPlayer.CharacterAdded:Connect(function(ch)
    task.wait(0.5)
    local hu = ch:FindFirstChildOfClass("Humanoid")
    if hu then 
        hu.AutoRotate = true 
        if Settings.WalkSpeedBoost then hu.WalkSpeed = Settings.WalkSpeedValue end
        if Settings.JumpPowerBoost then hu.JumpPower = Settings.JumpPowerValue end
    end
    if Settings.ChamsEnabled then task.wait(0.5); ApplyChams(LocalPlayer) end
end)

for _, p in ipairs(Players:GetPlayers()) do
    if p ~= LocalPlayer then CreateESP(p) end
end
Players.PlayerAdded:Connect(function(p)
    if p == LocalPlayer then return end
    CreateESP(p)
    p.CharacterAdded:Connect(function()
        task.wait(1)
        if Settings.ChamsEnabled then ApplyChams(p) end
        if Settings.GlowEnabled then ApplyGlow(p) end
    end)
end)
Players.PlayerRemoving:Connect(function(p)
    local esp = ESPData[p]
    if esp then
        for _, group in pairs({esp.BoxLines, esp.CornerLines, esp.Box3DLines, esp.SkeletonLines}) do
            for _, l in ipairs(group) do if l then pcall(function() l:Remove() end) end end
        end
        for _, k in ipairs({"NameText","DistText","WeaponText","HpBG","HpFill","Tracer","SnapLine","DormantMarker"}) do
            if esp[k] then pcall(function() esp[k]:Remove() end) end
        end
    end
    ESPData[p] = nil
    RemoveChams(p); RemoveGlow(p)
end)

-- ========== GUI SETUP ==========
for _, g in ipairs(CoreGui:GetChildren()) do
    if g.Name == "AimbotPROv13" or g.Name == "AimbotPROv12" or g.Name == "AimbotPROv11" then g:Destroy() end
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AimbotPROv13"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = CoreGui

-- Weather (NO GUI PARTICLES)
local WeatherOverlay = Instance.new("Frame")
WeatherOverlay.Size = UDim2.new(1, 0, 1, 0)
WeatherOverlay.BackgroundTransparency = 1
WeatherOverlay.ClipsDescendants = true
WeatherOverlay.ZIndex = 999
WeatherOverlay.Parent = ScreenGui

local WeatherParticles = {}
local function ClearWeather()
    for _, p in ipairs(WeatherParticles) do if p.frame then p.frame:Destroy() end end
    WeatherParticles = {}
end
local function InitWeather()
    ClearWeather()
    if Settings.WeatherType == "None" then return end
    local count = math.floor(Settings.WeatherIntensity * 1.5)
    for i = 1, count do
        local p = Instance.new("Frame")
        if Settings.WeatherType == "Snow" then
            local sz = math.random(3, 7)
            p.Size = UDim2.new(0, sz, 0, sz)
            p.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            p.BackgroundTransparency = math.random() * 0.3
            Instance.new("UICorner", p).CornerRadius = UDim.new(1, 0)
        elseif Settings.WeatherType == "Rain" then
            p.Size = UDim2.new(0, 2, 0, math.random(15, 25))
            p.BackgroundColor3 = Color3.fromRGB(150, 180, 255)
            p.BackgroundTransparency = 0.4
        elseif Settings.WeatherType == "Lightning" then
            local sz = math.random(2, 4)
            p.Size = UDim2.new(0, sz, 0, sz)
            p.BackgroundColor3 = Color3.fromRGB(200, 180, 255)
            p.BackgroundTransparency = math.random() * 0.5
            Instance.new("UICorner", p).CornerRadius = UDim.new(1, 0)
        end
        p.Position = UDim2.new(math.random(), 0, math.random() - 1, 0)
        p.BorderSizePixel = 0; p.ZIndex = 999; p.Parent = WeatherOverlay
        table.insert(WeatherParticles, {
            frame = p,
            vx = Settings.WeatherType == "Rain" and 0.05 or (math.random() - 0.5) * 0.1,
            vy = Settings.WeatherType == "Rain" and (0.5 + math.random() * 0.3) or (0.05 + math.random() * 0.08),
        })
    end
end

local LightningFlash = Instance.new("Frame")
LightningFlash.Size = UDim2.new(1, 0, 1, 0)
LightningFlash.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
LightningFlash.BackgroundTransparency = 1
LightningFlash.BorderSizePixel = 0
LightningFlash.ZIndex = 998
LightningFlash.Parent = ScreenGui

task.spawn(function()
    while true do
        task.wait(math.random(5, 12))
        if Settings.WeatherType == "Lightning" then
            for i = 1, math.random(2, 4) do
                LightningFlash.BackgroundTransparency = 0.3
                task.wait(0.05)
                LightningFlash.BackgroundTransparency = 1
                task.wait(0.08)
            end
        end
    end
end)

-- ========== TOGGLE BUTTON ==========
local ToggleButton = Instance.new("TextButton")
ToggleButton.Size = UDim2.new(0, 60, 0, 60)
ToggleButton.Position = UDim2.new(0, 18, 0.5, -30)
ToggleButton.BackgroundColor3 = Color3.fromRGB(20, 10, 40)
ToggleButton.Text = "⚡"
ToggleButton.TextColor3 = Color3.fromRGB(255, 220, 80)
ToggleButton.TextSize = 32
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.BorderSizePixel = 0
ToggleButton.ZIndex = 500
ToggleButton.Parent = ScreenGui
Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(1, 0)
local ToggleStroke = Instance.new("UIStroke", ToggleButton)
ToggleStroke.Color = Color3.fromRGB(180, 100, 255); ToggleStroke.Thickness = 3; ToggleStroke.Transparency = 0.2

local TGrad = Instance.new("UIGradient", ToggleButton)
TGrad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(60, 20, 120)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 10, 40))
}
TGrad.Rotation = 45

local LockIcon = Instance.new("TextLabel")
LockIcon.Size = UDim2.new(0, 18, 0, 18)
LockIcon.Position = UDim2.new(1, -18, 0, 0)
LockIcon.BackgroundColor3 = Color3.fromRGB(255, 180, 50)
LockIcon.Text = "🔒"
LockIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
LockIcon.TextSize = 10
LockIcon.Font = Enum.Font.GothamBold
LockIcon.BorderSizePixel = 0
LockIcon.Visible = false
LockIcon.ZIndex = 501
LockIcon.Parent = ToggleButton
Instance.new("UICorner", LockIcon).CornerRadius = UDim.new(1, 0)

-- ========== MAIN FRAME (WITH SCALE SUPPORT) ==========
local BASE_WIDTH = 820
local BASE_HEIGHT = 540

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, BASE_WIDTH, 0, BASE_HEIGHT)
MainFrame.Position = UDim2.new(0.5, -BASE_WIDTH/2, 0.5, -BASE_HEIGHT/2)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 5, 20)
MainFrame.BorderSizePixel = 0
MainFrame.Visible = false
MainFrame.ZIndex = 10
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 20)

local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Color = Color3.fromRGB(180, 100, 255); MainStroke.Thickness = 2; MainStroke.Transparency = 0

local MainGrad = Instance.new("UIGradient", MainFrame)
MainGrad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(35, 12, 70)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(10, 5, 20)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(25, 8, 55))
}
MainGrad.Rotation = 135

local GlowFrame = Instance.new("Frame")
GlowFrame.Size = UDim2.new(1, 30, 1, 30)
GlowFrame.Position = UDim2.new(0, -15, 0, -15)
GlowFrame.BackgroundTransparency = 1
GlowFrame.ZIndex = 9
GlowFrame.Parent = MainFrame
local glowStroke = Instance.new("UIStroke", GlowFrame)
glowStroke.Color = Color3.fromRGB(180, 100, 255); glowStroke.Thickness = 12; glowStroke.Transparency = 0.85
Instance.new("UICorner", GlowFrame).CornerRadius = UDim.new(0, 25)

-- Title Bar
local TitleBar = Instance.new("Frame")
TitleBar.Size = UDim2.new(1, 0, 0, 52)
TitleBar.BackgroundColor3 = Color3.fromRGB(20, 10, 45)
TitleBar.BackgroundTransparency = 0.1
TitleBar.BorderSizePixel = 0
TitleBar.ZIndex = 20
TitleBar.Parent = MainFrame
Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 20)

local TFix = Instance.new("Frame")
TFix.Size = UDim2.new(1, 0, 0.5, 0)
TFix.Position = UDim2.new(0, 0, 0.5, 0)
TFix.BackgroundColor3 = Color3.fromRGB(20, 10, 45)
TFix.BackgroundTransparency = 0.1
TFix.BorderSizePixel = 0
TFix.ZIndex = 20
TFix.Parent = TitleBar

local TitleGrad = Instance.new("UIGradient", TitleBar)
TitleGrad.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(80, 30, 160)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(40, 15, 90)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(80, 30, 160))
}

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(0.5, 0, 1, 0)
TitleLabel.Position = UDim2.new(0, 22, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = "✦ AIMBOT PRO"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 18
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.ZIndex = 21
TitleLabel.Parent = TitleBar

local VerLabel = Instance.new("TextLabel")
VerLabel.Size = UDim2.new(0, 90, 0, 20)
VerLabel.Position = UDim2.new(0, 160, 0.5, -10)
VerLabel.BackgroundColor3 = Color3.fromRGB(160, 60, 255)
VerLabel.Text = "v13.2 SAFE"
VerLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
VerLabel.TextSize = 10
VerLabel.Font = Enum.Font.GothamBold
VerLabel.BorderSizePixel = 0
VerLabel.ZIndex = 21
VerLabel.Parent = TitleBar
Instance.new("UICorner", VerLabel).CornerRadius = UDim.new(0, 7)
Instance.new("UIStroke", VerLabel).Color = Color3.fromRGB(255, 255, 255)
Instance.new("UIStroke", VerLabel).Thickness = 0.5

local HeartLabel = Instance.new("TextLabel")
HeartLabel.Size = UDim2.new(0, 36, 0, 36)
HeartLabel.Position = UDim2.new(0.5, -18, 0, 8)
HeartLabel.BackgroundTransparency = 1
HeartLabel.Text = "❤"
HeartLabel.TextColor3 = Color3.fromRGB(255, 80, 180)
HeartLabel.TextSize = 28
HeartLabel.Font = Enum.Font.GothamBold
HeartLabel.ZIndex = 21
HeartLabel.Parent = TitleBar

local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 34, 0, 34)
CloseButton.Position = UDim2.new(1, -44, 0, 9)
CloseButton.BackgroundColor3 = Color3.fromRGB(210, 40, 40)
CloseButton.Text = "✕"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 16
CloseButton.Font = Enum.Font.GothamBold
CloseButton.BorderSizePixel = 0
CloseButton.ZIndex = 22
CloseButton.Parent = TitleBar
Instance.new("UICorner", CloseButton).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", CloseButton).Color = Color3.fromRGB(255, 100, 100)
Instance.new("UIStroke", CloseButton).Thickness = 1

local MinButton = Instance.new("TextButton")
MinButton.Size = UDim2.new(0, 34, 0, 34)
MinButton.Position = UDim2.new(1, -84, 0, 9)
MinButton.BackgroundColor3 = Color3.fromRGB(60, 100, 200)
MinButton.Text = "−"
MinButton.TextColor3 = Color3.fromRGB(255, 255, 255)
MinButton.TextSize = 18
MinButton.Font = Enum.Font.GothamBold
MinButton.BorderSizePixel = 0
MinButton.ZIndex = 22
MinButton.Parent = TitleBar
Instance.new("UICorner", MinButton).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", MinButton).Color = Color3.fromRGB(100, 150, 255)
Instance.new("UIStroke", MinButton).Thickness = 1

local minimized = false

-- ========== TABS ==========
local Tabs = {"Aimbot", "Crosshair", "Anti-Aim", "ESP", "Visuals", "World", "Weather", "Movement", "Extra", "Inventory", "Indicator", "Colors", "Settings"}
local TabIcons = {"🎯", "✦", "🔄", "👁", "✨", "🌍", "⛅", "🏃", "🛠", "🎒", "📊", "🎨", "⚙"}

local TabBar = Instance.new("ScrollingFrame")
TabBar.Size = UDim2.new(0, 165, 1, -68)
TabBar.Position = UDim2.new(0, 10, 0, 58)
TabBar.BackgroundColor3 = Color3.fromRGB(15, 8, 30)
TabBar.BackgroundTransparency = 0.15
TabBar.BorderSizePixel = 0
TabBar.ScrollBarThickness = 3
TabBar.ScrollBarImageColor3 = Color3.fromRGB(180, 100, 255)
TabBar.CanvasSize = UDim2.new(0, 0, 0, #Tabs * 44 + 10)
TabBar.ZIndex = 20
TabBar.Parent = MainFrame
Instance.new("UICorner", TabBar).CornerRadius = UDim.new(0, 14)
Instance.new("UIStroke", TabBar).Color = Color3.fromRGB(60, 30, 100)
Instance.new("UIStroke", TabBar).Thickness = 1

local ContentArea = Instance.new("Frame")
ContentArea.Size = UDim2.new(1, -185, 1, -68)
ContentArea.Position = UDim2.new(0, 183, 0, 58)
ContentArea.BackgroundColor3 = Color3.fromRGB(15, 8, 30)
ContentArea.BackgroundTransparency = 0.15
ContentArea.BorderSizePixel = 0
ContentArea.ZIndex = 20
ContentArea.Parent = MainFrame
Instance.new("UICorner", ContentArea).CornerRadius = UDim.new(0, 14)
Instance.new("UIStroke", ContentArea).Color = Color3.fromRGB(60, 30, 100)
Instance.new("UIStroke", ContentArea).Thickness = 1

local TabButtons = {}
local TabPages = {}
for i, tabName in ipairs(Tabs) do
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -14, 0, 38)
    btn.Position = UDim2.new(0, 7, 0, 5 + (i-1) * 42)
    btn.BackgroundColor3 = Color3.fromRGB(25, 15, 50)
    btn.Text = "  " .. TabIcons[i] .. "  " .. tabName
    btn.TextColor3 = Color3.fromRGB(180, 180, 220)
    btn.TextSize = 12
    btn.Font = Enum.Font.GothamBold
    btn.TextXAlignment = Enum.TextXAlignment.Left
    btn.BorderSizePixel = 0
    btn.ZIndex = 21
    btn.Parent = TabBar
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
    TabButtons[i] = btn

    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1, -14, 1, -14)
    page.Position = UDim2.new(0, 7, 0, 7)
    page.BackgroundTransparency = 1
    page.BorderSizePixel = 0
    page.ScrollBarThickness = 4
    page.ScrollBarImageColor3 = Color3.fromRGB(180, 100, 255)
    page.CanvasSize = UDim2.new(0, 0, 0, 800)
    page.Visible = false
    page.ZIndex = 21
    page.Parent = ContentArea
    TabPages[i] = page

    btn.MouseButton1Click:Connect(function()
        for j, b in ipairs(TabButtons) do
            TweenService:Create(b, TweenInfo.new(0.2, Enum.EasingStyle.Quart), {
                BackgroundColor3 = Color3.fromRGB(25, 15, 50),
                TextColor3 = Color3.fromRGB(180, 180, 220)
            }):Play()
            TabPages[j].Visible = false
        end
        TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Quart), {
            BackgroundColor3 = Color3.fromRGB(140, 70, 240),
            TextColor3 = Color3.fromRGB(255, 255, 255)
        }):Play()
        page.Visible = true
    end)
end
TabButtons[1].BackgroundColor3 = Color3.fromRGB(140, 70, 240)
TabButtons[1].TextColor3 = Color3.fromRGB(255, 255, 255)
TabPages[1].Visible = true

-- ========== GUI SCALE SYSTEM ==========
local function ApplyGUIScale()
    local scale = Settings.GUIScale / 100
    local newW = BASE_WIDTH * scale
    local newH = BASE_HEIGHT * scale
    MainFrame.Size = UDim2.new(0, newW, 0, newH)
    MainFrame.Position = UDim2.new(0.5, -newW/2, 0.5, -newH/2)
    -- Scale toggle button too
    local togScale = math.clamp(scale, 0.5, 1.5)
    ToggleButton.Size = UDim2.new(0, 60 * togScale, 0, 60 * togScale)
end

-- ========== HELPERS ==========
local function CreateSection(parent, text, y)
    local s = Instance.new("Frame")
    s.Size = UDim2.new(1, -10, 0, 28)
    s.Position = UDim2.new(0, 5, 0, y)
    s.BackgroundColor3 = Color3.fromRGB(40, 20, 75)
    s.BorderSizePixel = 0
    s.ZIndex = 21
    s.Parent = parent
    Instance.new("UICorner", s).CornerRadius = UDim.new(0, 7)
    local sg = Instance.new("UIGradient", s)
    sg.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(80, 35, 140)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(30, 15, 60))
    }
    sg.Rotation = 90
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, -20, 1, 0)
    lbl.Position = UDim2.new(0, 14, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(255, 220, 255)
    lbl.TextSize = 12
    lbl.Font = Enum.Font.GothamBold
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.ZIndex = 22
    lbl.Parent = s
end

local function CreateLabel(parent, text, x, y, w, h, size, color)
    local l = Instance.new("TextLabel")
    l.Size = UDim2.new(0, w, 0, h)
    l.Position = UDim2.new(0, x, 0, y)
    l.BackgroundTransparency = 1
    l.Text = text
    l.TextColor3 = color or Color3.fromRGB(200, 200, 220)
    l.TextSize = size or 12
    l.Font = Enum.Font.Gotham
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.ZIndex = 22
    l.Parent = parent
    return l
end

local function CreateToggle(parent, text, y, default, callback)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1, -10, 0, 32)
    f.Position = UDim2.new(0, 5, 0, y)
    f.BackgroundTransparency = 1
    f.ZIndex = 21
    f.Parent = parent
    CreateLabel(f, text, 5, 7, 300, 18)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 50, 0, 24)
    btn.Position = UDim2.new(1, -55, 0, 4)
    btn.BackgroundColor3 = default and Color3.fromRGB(80, 220, 120) or Color3.fromRGB(60, 40, 90)
    btn.Text = default and "ON" or "OFF"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 11
    btn.Font = Enum.Font.GothamBold
    btn.BorderSizePixel = 0
    btn.ZIndex = 22
    btn.Parent = f
    Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 0)
    local s = default
    btn.MouseButton1Click:Connect(function()
        s = not s
        TweenService:Create(btn, TweenInfo.new(0.2, Enum.EasingStyle.Quart), {
            BackgroundColor3 = s and Color3.fromRGB(80, 220, 120) or Color3.fromRGB(60, 40, 90)
        }):Play()
        btn.Text = s and "ON" or "OFF"
        callback(s)
    end)
end

local function CreateSlider(parent, text, y, min, max, default, callback)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1, -10, 0, 48)
    f.Position = UDim2.new(0, 5, 0, y)
    f.BackgroundTransparency = 1
    f.ZIndex = 21
    f.Parent = parent
    CreateLabel(f, text, 5, 0, 300, 18)
    local vL = Instance.new("TextLabel")
    vL.Size = UDim2.new(0, 60, 0, 18)
    vL.Position = UDim2.new(1, -65, 0, 0)
    vL.BackgroundTransparency = 1
    vL.Text = tostring(default)
    vL.TextColor3 = Color3.fromRGB(255, 220, 255)
    vL.TextSize = 12
    vL.Font = Enum.Font.GothamBold
    vL.TextXAlignment = Enum.TextXAlignment.Right
    vL.ZIndex = 22
    vL.Parent = f
    local tr = Instance.new("Frame")
    tr.Size = UDim2.new(1, -10, 0, 8)
    tr.Position = UDim2.new(0, 5, 0, 28)
    tr.BackgroundColor3 = Color3.fromRGB(35, 22, 60)
    tr.BorderSizePixel = 0
    tr.ZIndex = 22
    tr.Parent = f
    Instance.new("UICorner", tr).CornerRadius = UDim.new(1, 0)
    local fl = Instance.new("Frame")
    fl.Size = UDim2.new((default - min) / (max - min), 0, 1, 0)
    fl.BackgroundColor3 = Color3.fromRGB(180, 100, 255)
    fl.BorderSizePixel = 0
    fl.ZIndex = 23
    fl.Parent = tr
    Instance.new("UICorner", fl).CornerRadius = UDim.new(1, 0)
    local flGrad = Instance.new("UIGradient", fl)
    flGrad.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 100, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 80, 255))
    }
    local kn = Instance.new("Frame")
    kn.Size = UDim2.new(0, 16, 0, 16)
    kn.Position = UDim2.new((default - min) / (max - min), -8, 0.5, -8)
    kn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    kn.BorderSizePixel = 0
    kn.ZIndex = 24
    kn.Parent = tr
    Instance.new("UICorner", kn).CornerRadius = UDim.new(1, 0)
    local ks = Instance.new("UIStroke", kn)
    ks.Color = Color3.fromRGB(180, 100, 255); ks.Thickness = 2.5

    local dr = false
    local function upd(x)
        local r = math.clamp((x - tr.AbsolutePosition.X) / tr.AbsoluteSize.X, 0, 1)
        local val = math.floor(min + (max - min) * r)
        fl.Size = UDim2.new(r, 0, 1, 0)
        kn.Position = UDim2.new(r, -8, 0.5, -8)
        vL.Text = tostring(val)
        callback(val)
    end
    tr.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
            dr = true; upd(i.Position.X)
        end
    end)
    UserInputService.InputChanged:Connect(function(i)
        if dr and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
            upd(i.Position.X)
        end
    end)
    UserInputService.InputEnded:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then dr = false end
    end)
end

local function CreateDropdown(parent, text, y, options, default, callback)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1, -10, 0, 32)
    f.Position = UDim2.new(0, 5, 0, y)
    f.BackgroundTransparency = 1
    f.ZIndex = 21
    f.Parent = parent
    CreateLabel(f, text, 5, 7, 200, 18)
    local idx = 1
    for i, o in ipairs(options) do if o == default then idx = i end end
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 145, 0, 26)
    btn.Position = UDim2.new(1, -150, 0, 3)
    btn.BackgroundColor3 = Color3.fromRGB(60, 30, 110)
    btn.Text = "▼ " .. options[idx]
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 11
    btn.Font = Enum.Font.GothamBold
    btn.BorderSizePixel = 0
    btn.ZIndex = 22
    btn.Parent = f
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 7)
    Instance.new("UIStroke", btn).Color = Color3.fromRGB(120, 60, 200)
    Instance.new("UIStroke", btn).Thickness = 0.5
    btn.MouseButton1Click:Connect(function()
        idx = idx + 1
        if idx > #options then idx = 1 end
        btn.Text = "▼ " .. options[idx]
        callback(options[idx])
    end)
end

local ActivePicker = nil
local function CreateColorPicker(parent, text, y, default, callback)
    local f = Instance.new("Frame")
    f.Size = UDim2.new(1, -10, 0, 32)
    f.Position = UDim2.new(0, 5, 0, y)
    f.BackgroundTransparency = 1
    f.ZIndex = 21
    f.Parent = parent
    CreateLabel(f, text, 5, 7, 200, 18)
    local prv = Instance.new("TextButton")
    prv.Size = UDim2.new(0, 65, 0, 24)
    prv.Position = UDim2.new(1, -70, 0, 4)
    prv.BackgroundColor3 = C3(default)
    prv.Text = ""
    prv.BorderSizePixel = 0
    prv.ZIndex = 22
    prv.Parent = f
    Instance.new("UICorner", prv).CornerRadius = UDim.new(0, 6)
    local ps = Instance.new("UIStroke", prv)
    ps.Color = Color3.fromRGB(255, 255, 255); ps.Thickness = 1.5
    local cur = {default[1], default[2], default[3]}

    prv.MouseButton1Click:Connect(function()
        if ActivePicker then ActivePicker:Destroy() end
        local pk = Instance.new("Frame")
        pk.Size = UDim2.new(0, 280, 0, 320)
        pk.Position = UDim2.new(0.5, -140, 0.5, -160)
        pk.BackgroundColor3 = Color3.fromRGB(15, 8, 30)
        pk.BorderSizePixel = 0
        pk.ZIndex = 800
        pk.Parent = ScreenGui
        Instance.new("UICorner", pk).CornerRadius = UDim.new(0, 14)
        local pkS = Instance.new("UIStroke", pk)
        pkS.Color = Color3.fromRGB(180, 100, 255); pkS.Thickness = 2
        local pkG = Instance.new("UIGradient", pk)
        pkG.Color = ColorSequence.new{
            ColorSequenceKeypoint.new(0, Color3.fromRGB(40, 18, 75)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(12, 6, 25))
        }
        pkG.Rotation = 135
        ActivePicker = pk

        local tt = Instance.new("TextLabel")
        tt.Size = UDim2.new(1, -40, 0, 30)
        tt.Position = UDim2.new(0, 14, 0, 8)
        tt.BackgroundTransparency = 1
        tt.Text = "🎨 " .. text
        tt.TextColor3 = Color3.fromRGB(255, 255, 255)
        tt.TextSize = 15
        tt.Font = Enum.Font.GothamBold
        tt.TextXAlignment = Enum.TextXAlignment.Left
        tt.ZIndex = 801
        tt.Parent = pk
        local cb = Instance.new("TextButton")
        cb.Size = UDim2.new(0, 26, 0, 26)
        cb.Position = UDim2.new(1, -32, 0, 10)
        cb.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        cb.Text = "✕"
        cb.TextColor3 = Color3.fromRGB(255, 255, 255)
        cb.TextSize = 13
        cb.Font = Enum.Font.GothamBold
        cb.BorderSizePixel = 0
        cb.ZIndex = 802
        cb.Parent = pk
        Instance.new("UICorner", cb).CornerRadius = UDim.new(1, 0)
        local pv2 = Instance.new("Frame")
        pv2.Size = UDim2.new(1, -28, 0, 38)
        pv2.Position = UDim2.new(0, 14, 0, 48)
        pv2.BackgroundColor3 = C3(cur)
        pv2.BorderSizePixel = 0
        pv2.ZIndex = 801
        pv2.Parent = pk
        Instance.new("UICorner", pv2).CornerRadius = UDim.new(0, 7)
        Instance.new("UIStroke", pv2).Color = Color3.fromRGB(255, 255, 255)
        Instance.new("UIStroke", pv2).Thickness = 1

        local function mk(nm, yy, col, dv, cbk)
            local lb = Instance.new("TextLabel")
            lb.Size = UDim2.new(0, 20, 0, 20)
            lb.Position = UDim2.new(0, 14, 0, yy)
            lb.BackgroundTransparency = 1
            lb.Text = nm; lb.TextColor3 = col; lb.TextSize = 14
            lb.Font = Enum.Font.GothamBold; lb.ZIndex = 801; lb.Parent = pk
            local vv = Instance.new("TextLabel")
            vv.Size = UDim2.new(0, 40, 0, 20)
            vv.Position = UDim2.new(1, -54, 0, yy)
            vv.BackgroundTransparency = 1
            vv.Text = tostring(dv); vv.TextColor3 = Color3.fromRGB(255,255,255)
            vv.TextSize = 12; vv.Font = Enum.Font.GothamBold; vv.ZIndex = 801; vv.Parent = pk
            local tr = Instance.new("Frame")
            tr.Size = UDim2.new(1, -96, 0, 9)
            tr.Position = UDim2.new(0, 40, 0, yy + 6)
            tr.BackgroundColor3 = Color3.fromRGB(30, 20, 50)
            tr.BorderSizePixel = 0; tr.ZIndex = 801; tr.Parent = pk
            Instance.new("UICorner", tr).CornerRadius = UDim.new(1, 0)
            local fl = Instance.new("Frame")
            fl.Size = UDim2.new(dv/255, 0, 1, 0)
            fl.BackgroundColor3 = col
            fl.BorderSizePixel = 0; fl.ZIndex = 802; fl.Parent = tr
            Instance.new("UICorner", fl).CornerRadius = UDim.new(1, 0)
            local dr = false
            local function up(x)
                local r = math.clamp((x - tr.AbsolutePosition.X) / tr.AbsoluteSize.X, 0, 1)
                local vl = math.floor(r * 255)
                fl.Size = UDim2.new(r, 0, 1, 0); vv.Text = tostring(vl); cbk(vl)
            end
            tr.InputBegan:Connect(function(i)
                if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                    dr = true; up(i.Position.X)
                end
            end)
            UserInputService.InputChanged:Connect(function(i)
                if dr and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
                    up(i.Position.X)
                end
            end)
            UserInputService.InputEnded:Connect(function(i)
                if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then dr = false end
            end)
        end
        mk("R", 100, Color3.fromRGB(255,70,70), cur[1], function(v) cur[1]=v; pv2.BackgroundColor3=C3(cur); prv.BackgroundColor3=C3(cur); callback(cur) end)
        mk("G", 138, Color3.fromRGB(70,255,70), cur[2], function(v) cur[2]=v; pv2.BackgroundColor3=C3(cur); prv.BackgroundColor3=C3(cur); callback(cur) end)
        mk("B", 176, Color3.fromRGB(70,150,255), cur[3], function(v) cur[3]=v; pv2.BackgroundColor3=C3(cur); prv.BackgroundColor3=C3(cur); callback(cur) end)

        local presets = {{255,255,255},{0,0,0},{255,0,0},{0,255,0},{0,0,255},{255,255,0},{255,0,255},{0,255,255},{255,128,0},{180,100,255}}
        CreateLabel(pk, "Presets:", 14, 215, 100, 16, 11, Color3.fromRGB(220, 180, 255))
        for i, col in ipairs(presets) do
            local x = 14 + ((i-1) % 5) * 52
            local yy = 238 + math.floor((i-1) / 5) * 36
            local pb = Instance.new("TextButton")
            pb.Size = UDim2.new(0, 48, 0, 30)
            pb.Position = UDim2.new(0, x, 0, yy)
            pb.BackgroundColor3 = C3(col); pb.Text = ""
            pb.BorderSizePixel = 0; pb.ZIndex = 802; pb.Parent = pk
            Instance.new("UICorner", pb).CornerRadius = UDim.new(0, 6)
            Instance.new("UIStroke", pb).Color = Color3.fromRGB(255, 255, 255)
            Instance.new("UIStroke", pb).Thickness = 0.5
            pb.MouseButton1Click:Connect(function()
                cur = {col[1], col[2], col[3]}
                pv2.BackgroundColor3 = C3(cur); prv.BackgroundColor3 = C3(cur)
                callback(cur)
            end)
        end
        cb.MouseButton1Click:Connect(function()
            pk:Destroy()
            if ActivePicker == pk then ActivePicker = nil end
        end)
        local dr, ds, sp = false
        tt.InputBegan:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                dr = true; ds = i.Position; sp = pk.Position
            end
        end)
        UserInputService.InputChanged:Connect(function(i)
            if dr and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
                local d = i.Position - ds
                pk.Position = UDim2.new(sp.X.Scale, sp.X.Offset + d.X, sp.Y.Scale, sp.Y.Offset + d.Y)
            end
        end)
        UserInputService.InputEnded:Connect(function(i)
            if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then dr = false end
        end)
    end)
end

local function CreateButton(parent, text, y, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 34)
    btn.Position = UDim2.new(0, 5, 0, y)
    btn.BackgroundColor3 = Color3.fromRGB(110, 60, 220)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 12
    btn.Font = Enum.Font.GothamBold
    btn.BorderSizePixel = 0
    btn.ZIndex = 21
    btn.Parent = parent
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 9)
    local bs = Instance.new("UIStroke", btn)
    bs.Color = Color3.fromRGB(180, 100, 255); bs.Thickness = 1; bs.Transparency = 0.5
    btn.MouseButton1Click:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(170, 100, 255)}):Play()
        task.wait(0.1)
        TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(110, 60, 220)}):Play()
        callback()
    end)
end

local function Notify(text)
    local n = Instance.new("Frame")
    n.Size = UDim2.new(0, 300, 0, 50)
    n.Position = UDim2.new(0.5, -150, 1, 20)
    n.BackgroundColor3 = Color3.fromRGB(20, 10, 45)
    n.BorderSizePixel = 0
    n.ZIndex = 900
    n.Parent = ScreenGui
    Instance.new("UICorner", n).CornerRadius = UDim.new(0, 12)
    local ns = Instance.new("UIStroke", n)
    ns.Color = Color3.fromRGB(180, 100, 255); ns.Thickness = 2
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text
    lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
    lbl.TextSize = 13
    lbl.Font = Enum.Font.GothamBold
    lbl.ZIndex = 901
    lbl.Parent = n
    TweenService:Create(n, TweenInfo.new(0.4, Enum.EasingStyle.Back), {Position = UDim2.new(0.5, -150, 1, -75)}):Play()
    task.delay(2.5, function()
        TweenService:Create(n, TweenInfo.new(0.3), {Position = UDim2.new(0.5, -150, 1, 20), BackgroundTransparency = 1}):Play()
        task.delay(0.4, function() n:Destroy() end)
    end)
end

-- ========== RADAR ==========
local RadarFrame = Instance.new("Frame")
RadarFrame.Size = UDim2.new(0, Settings.RadarSize, 0, Settings.RadarSize)
RadarFrame.Position = UDim2.new(1, -Settings.RadarSize - 20, 0, 20)
RadarFrame.BackgroundColor3 = Color3.fromRGB(12, 6, 25)
RadarFrame.BackgroundTransparency = 0.2
RadarFrame.BorderSizePixel = 0
RadarFrame.Visible = false
RadarFrame.ZIndex = 100
RadarFrame.Parent = ScreenGui
Instance.new("UICorner", RadarFrame).CornerRadius = UDim.new(1, 0)
local RadarStroke = Instance.new("UIStroke", RadarFrame)
RadarStroke.Color = Color3.fromRGB(180, 100, 255); RadarStroke.Thickness = 2

local RadarCross1 = Instance.new("Frame")
RadarCross1.Size = UDim2.new(1, 0, 0, 1)
RadarCross1.Position = UDim2.new(0, 0, 0.5, 0)
RadarCross1.BackgroundColor3 = Color3.fromRGB(180, 100, 255)
RadarCross1.BackgroundTransparency = 0.5
RadarCross1.BorderSizePixel = 0
RadarCross1.ZIndex = 101
RadarCross1.Parent = RadarFrame
local RadarCross2 = Instance.new("Frame")
RadarCross2.Size = UDim2.new(0, 1, 1, 0)
RadarCross2.Position = UDim2.new(0.5, 0, 0, 0)
RadarCross2.BackgroundColor3 = Color3.fromRGB(180, 100, 255)
RadarCross2.BackgroundTransparency = 0.5
RadarCross2.BorderSizePixel = 0
RadarCross2.ZIndex = 101
RadarCross2.Parent = RadarFrame

local RadarCenter = Instance.new("Frame")
RadarCenter.Size = UDim2.new(0, 8, 0, 8)
RadarCenter.Position = UDim2.new(0.5, -4, 0.5, -4)
RadarCenter.BackgroundColor3 = Color3.fromRGB(80, 255, 120)
RadarCenter.BorderSizePixel = 0
RadarCenter.ZIndex = 102
RadarCenter.Parent = RadarFrame
Instance.new("UICorner", RadarCenter).CornerRadius = UDim.new(1, 0)
Instance.new("UIStroke", RadarCenter).Color = Color3.fromRGB(255, 255, 255)
Instance.new("UIStroke", RadarCenter).Thickness = 1

local RadarDots = {}
local function GetRadarDot(player)
    if not RadarDots[player] then
        local d = Instance.new("Frame")
        d.Size = UDim2.new(0, 7, 0, 7)
        d.BackgroundColor3 = C3(Settings.ESPColor)
        d.BorderSizePixel = 0
        d.ZIndex = 102
        d.Parent = RadarFrame
        Instance.new("UICorner", d).CornerRadius = UDim.new(1, 0)
        RadarDots[player] = d
    end
    return RadarDots[player]
end

local StatsHUD = Instance.new("Frame")
StatsHUD.Size = UDim2.new(0, 160, 0, 90)
StatsHUD.Position = UDim2.new(0, 20, 1, -110)
StatsHUD.BackgroundColor3 = Color3.fromRGB(12, 6, 25)
StatsHUD.BackgroundTransparency = 0.2
StatsHUD.BorderSizePixel = 0
StatsHUD.Visible = false
StatsHUD.ZIndex = 100
StatsHUD.Parent = ScreenGui
Instance.new("UICorner", StatsHUD).CornerRadius = UDim.new(0, 10)
local StatsHUDStroke = Instance.new("UIStroke", StatsHUD)
StatsHUDStroke.Color = Color3.fromRGB(180, 100, 255); StatsHUDStroke.Thickness = 1.5

local FPSLabel = Instance.new("TextLabel")
FPSLabel.Size = UDim2.new(1, -12, 0, 20)
FPSLabel.Position = UDim2.new(0, 6, 0, 4)
FPSLabel.BackgroundTransparency = 1
FPSLabel.Text = "FPS: 0"
FPSLabel.TextColor3 = Color3.fromRGB(80, 255, 120)
FPSLabel.TextSize = 12
FPSLabel.Font = Enum.Font.GothamBold
FPSLabel.TextXAlignment = Enum.TextXAlignment.Left
FPSLabel.ZIndex = 101
FPSLabel.Parent = StatsHUD

local MSLabel = Instance.new("TextLabel")
MSLabel.Size = UDim2.new(1, -12, 0, 20)
MSLabel.Position = UDim2.new(0, 6, 0, 26)
MSLabel.BackgroundTransparency = 1
MSLabel.Text = "MS: 0"
MSLabel.TextColor3 = Color3.fromRGB(255, 200, 80)
MSLabel.TextSize = 12
MSLabel.Font = Enum.Font.GothamBold
MSLabel.TextXAlignment = Enum.TextXAlignment.Left
MSLabel.ZIndex = 101
MSLabel.Parent = StatsHUD

local PingLabel = Instance.new("TextLabel")
PingLabel.Size = UDim2.new(1, -12, 0, 20)
PingLabel.Position = UDim2.new(0, 6, 0, 48)
PingLabel.BackgroundTransparency = 1
PingLabel.Text = "Ping: 0"
PingLabel.TextColor3 = Color3.fromRGB(255, 120, 200)
PingLabel.TextSize = 12
PingLabel.Font = Enum.Font.GothamBold
PingLabel.TextXAlignment = Enum.TextXAlignment.Left
PingLabel.ZIndex = 101
PingLabel.Parent = StatsHUD

local CompassFrame = Instance.new("Frame")
CompassFrame.Size = UDim2.new(0, 320, 0, 32)
CompassFrame.Position = UDim2.new(0.5, -160, 0, 12)
CompassFrame.BackgroundColor3 = Color3.fromRGB(12, 6, 25)
CompassFrame.BackgroundTransparency = 0.3
CompassFrame.BorderSizePixel = 0
CompassFrame.Visible = false
CompassFrame.ZIndex = 100
CompassFrame.ClipsDescendants = true
CompassFrame.Parent = ScreenGui
Instance.new("UICorner", CompassFrame).CornerRadius = UDim.new(0, 8)
local CompassStroke = Instance.new("UIStroke", CompassFrame)
CompassStroke.Color = Color3.fromRGB(180, 100, 255); CompassStroke.Thickness = 1.5

local CompassLabel = Instance.new("TextLabel")
CompassLabel.Size = UDim2.new(1, 0, 1, 0)
CompassLabel.BackgroundTransparency = 1
CompassLabel.Text = "N"
CompassLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
CompassLabel.TextSize = 14
CompassLabel.Font = Enum.Font.GothamBold
CompassLabel.ZIndex = 101
CompassLabel.Parent = CompassFrame

local NearbyAlert = Instance.new("Frame")
NearbyAlert.Size = UDim2.new(0, 220, 0, 42)
NearbyAlert.Position = UDim2.new(0.5, -110, 0, 55)
NearbyAlert.BackgroundColor3 = Color3.fromRGB(50, 10, 10)
NearbyAlert.BackgroundTransparency = 0.2
NearbyAlert.BorderSizePixel = 0
NearbyAlert.Visible = false
NearbyAlert.ZIndex = 100
NearbyAlert.Parent = ScreenGui
Instance.new("UICorner", NearbyAlert).CornerRadius = UDim.new(0, 10)
local NearbyStroke = Instance.new("UIStroke", NearbyAlert)
NearbyStroke.Color = Color3.fromRGB(255, 50, 50); NearbyStroke.Thickness = 2
local NearbyLabel = Instance.new("TextLabel")
NearbyLabel.Size = UDim2.new(1, 0, 1, 0)
NearbyLabel.BackgroundTransparency = 1
NearbyLabel.Text = "⚠ ENEMY NEARBY"
NearbyLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
NearbyLabel.TextSize = 14
NearbyLabel.Font = Enum.Font.GothamBold
NearbyLabel.ZIndex = 101
NearbyLabel.Parent = NearbyAlert

local TPCooldownFrame = Instance.new("Frame")
TPCooldownFrame.Size = UDim2.new(0, 140, 0, 32)
TPCooldownFrame.Position = UDim2.new(0, 20, 0, 20)
TPCooldownFrame.BackgroundColor3 = Color3.fromRGB(12, 6, 25)
TPCooldownFrame.BackgroundTransparency = 0.2
TPCooldownFrame.BorderSizePixel = 0
TPCooldownFrame.Visible = false
TPCooldownFrame.ZIndex = 100
TPCooldownFrame.Parent = ScreenGui
Instance.new("UICorner", TPCooldownFrame).CornerRadius = UDim.new(0, 7)
Instance.new("UIStroke", TPCooldownFrame).Color = Color3.fromRGB(180, 100, 255)
local TPCooldownLabel = Instance.new("TextLabel")
TPCooldownLabel.Size = UDim2.new(1, 0, 1, 0)
TPCooldownLabel.BackgroundTransparency = 1
TPCooldownLabel.Text = "TP: Ready"
TPCooldownLabel.TextColor3 = Color3.fromRGB(80, 255, 120)
TPCooldownLabel.TextSize = 12
TPCooldownLabel.Font = Enum.Font.GothamBold
TPCooldownLabel.ZIndex = 101
TPCooldownLabel.Parent = TPCooldownFrame

local InvFullFrame = Instance.new("Frame")
InvFullFrame.Size = UDim2.new(0, 200, 0, 32)
InvFullFrame.Position = UDim2.new(0, 20, 0, 60)
InvFullFrame.BackgroundColor3 = Color3.fromRGB(50, 30, 10)
InvFullFrame.BackgroundTransparency = 0.2
InvFullFrame.BorderSizePixel = 0
InvFullFrame.Visible = false
InvFullFrame.ZIndex = 100
InvFullFrame.Parent = ScreenGui
Instance.new("UICorner", InvFullFrame).CornerRadius = UDim.new(0, 7)
Instance.new("UIStroke", InvFullFrame).Color = Color3.fromRGB(255, 180, 50)
local InvFullLabel = Instance.new("TextLabel")
InvFullLabel.Size = UDim2.new(1, 0, 1, 0)
InvFullLabel.BackgroundTransparency = 1
InvFullLabel.Text = "⚠ Inventory Full"
InvFullLabel.TextColor3 = Color3.fromRGB(255, 180, 50)
InvFullLabel.TextSize = 12
InvFullLabel.Font = Enum.Font.GothamBold
InvFullLabel.ZIndex = 101
InvFullLabel.Parent = InvFullFrame

local AutoPeekIndicator = Instance.new("Frame")
AutoPeekIndicator.Size = UDim2.new(0, 130, 0, 32)
AutoPeekIndicator.Position = UDim2.new(0.5, -65, 0.7, 0)
AutoPeekIndicator.BackgroundColor3 = Color3.fromRGB(12, 6, 25)
AutoPeekIndicator.BackgroundTransparency = 0.2
AutoPeekIndicator.BorderSizePixel = 0
AutoPeekIndicator.Visible = false
AutoPeekIndicator.ZIndex = 100
AutoPeekIndicator.Parent = ScreenGui
Instance.new("UICorner", AutoPeekIndicator).CornerRadius = UDim.new(0, 7)
local APStroke = Instance.new("UIStroke", AutoPeekIndicator)
APStroke.Color = Color3.fromRGB(80, 255, 200); APStroke.Thickness = 1.5
local APLabel = Instance.new("TextLabel")
APLabel.Size = UDim2.new(1, 0, 1, 0)
APLabel.BackgroundTransparency = 1
APLabel.Text = "🎯 AUTO PEEK"
APLabel.TextColor3 = Color3.fromRGB(80, 255, 200)
APLabel.TextSize = 12
APLabel.Font = Enum.Font.GothamBold
APLabel.ZIndex = 101
APLabel.Parent = AutoPeekIndicator

local DamageTextContainer = Instance.new("Frame")
DamageTextContainer.Size = UDim2.new(1, 0, 1, 0)
DamageTextContainer.BackgroundTransparency = 1
DamageTextContainer.ZIndex = 200
DamageTextContainer.Parent = ScreenGui

local function ShowDamageText(pos, dmg)
    local sp, os = Camera:WorldToViewportPoint(pos)
    if not os then return end
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(0, 80, 0, 30)
    lbl.Position = UDim2.new(0, sp.X - 40, 0, sp.Y - 15)
    lbl.BackgroundTransparency = 1
    lbl.Text = "-" .. tostring(dmg)
    lbl.TextColor3 = C3(Settings.DamageTextColor)
    lbl.TextSize = 18
    lbl.Font = Enum.Font.GothamBold
    lbl.TextStrokeTransparency = 0
    lbl.ZIndex = 201
    lbl.Parent = DamageTextContainer
    TweenService:Create(lbl, TweenInfo.new(1), {
        Position = UDim2.new(0, sp.X - 40, 0, sp.Y - 60),
        TextTransparency = 1,
        TextStrokeTransparency = 1
    }):Play()
    task.delay(1.1, function() lbl:Destroy() end)
end

local HealthTracker = {}
local function TrackHealth(player)
    if player == LocalPlayer then return end
    task.spawn(function()
        while player.Parent do
            local ch = player.Character
            if ch then
                local hu = ch:FindFirstChildOfClass("Humanoid")
                if hu then
                    local prev = HealthTracker[player] or hu.Health
                    if hu.Health < prev - 0.5 and Settings.DamageText then
                        local dmg = math.floor(prev - hu.Health)
                        local head = ch:FindFirstChild("Head")
                        if head and dmg > 0 then ShowDamageText(head.Position, dmg) end
                    end
                    HealthTracker[player] = hu.Health
                end
            end
            task.wait(0.1)
        end
    end)
end
for _, p in ipairs(Players:GetPlayers()) do TrackHealth(p) end
Players.PlayerAdded:Connect(TrackHealth)

-- ========== CONFIG SYSTEM ==========
local CONFIG_NAME = "AimbotPROv13"
local ConfigSlots = {"Slot1", "Slot2", "Slot3", "Slot4", "Slot5"}
local CurrentConfigSlot = "Slot1"

local function EncodeSettings()
    local d = {}
    for k, v in pairs(Settings) do d[k] = v end
    return HttpService:JSONEncode(d)
end

local function DecodeSettings(str)
    local ok, d = pcall(function() return HttpService:JSONDecode(str) end)
    if ok and d then
        for k, v in pairs(d) do
            if Settings[k] ~= nil then
                if type(v) == "table" then Settings[k] = {v[1] or 0, v[2] or 0, v[3] or 0}
                else Settings[k] = v end
            end
        end
        return true
    end
    return false
end

local function ApplyAllSettings()
    ApplyWorldSettings()
    RefreshChams()
    if Settings.WeatherEnabled then InitWeather() else ClearWeather() end
    LockIcon.Visible = Settings.ToggleButtonLocked
    RadarFrame.Visible = Settings.RadarEnabled
    if FOVCircle then FOVCircle.Thickness = Settings.FOVThickness end
    ApplyGUIScale()
end

local function SaveConfig(name)
    local ok, err = pcall(function() writefile(name .. ".json", EncodeSettings()) end)
    return ok, err
end

local function LoadConfig(name)
    local ok, content = pcall(function() return readfile(name .. ".json") end)
    if not ok or not content then return false end
    if DecodeSettings(content) then
        ApplyAllSettings()
        return true
    end
    return false
end

local function DeleteConfig(name)
    pcall(function() delfile(name .. ".json") end)
end

-- ========== BUILD TABS ==========
-- Tab 1: AIMBOT
local at = TabPages[1]
CreateSection(at, "🎯 CORE", 5)
CreateToggle(at, "Aimbot Enabled", 38, false, function(s) Settings.AimbotEnabled = s end)
CreateDropdown(at, "Hitbox", 74, {"Head","Chest","Stomach","Pelvis","Body"}, "Head", function(v) Settings.AimbotHitbox = v end)
CreateSlider(at, "FOV Size", 110, 20, 600, 150, function(v) Settings.FOV = v end)
CreateSlider(at, "FOV Thickness", 162, 1, 10, 2, function(v) Settings.FOVThickness = v end)
CreateSlider(at, "Smoothness", 214, 0, 20, 5, function(v) Settings.Smoothness = v end)
CreateDropdown(at, "Aim Mode", 266, {"Hold","Toggle","Always"}, "Hold", function(v) Settings.AimbotMode = v end)
CreateSection(at, "🎯 MULTIPOINT & CHECKS", 308)
CreateToggle(at, "Multipoint Hitbox", 341, false, function(s) Settings.AimbotMultipoint = s end)
CreateSlider(at, "Multipoint Scale", 375, 0, 100, 50, function(v) Settings.AimbotMultipointScale = v end)
CreateToggle(at, "Team Check", 427, false, function(s) Settings.TeamCheck = s end)
CreateToggle(at, "Wall Check", 461, false, function(s) Settings.WallCheck = s end)
CreateSection(at, "🩸 BAIM", 503)
CreateToggle(at, "Baim Enabled", 536, false, function(s) Settings.BaimEnabled = s end)
CreateSlider(at, "Baim After X Misses", 570, 0, 10, 0, function(v) Settings.BaimAfterMisses = v end)
CreateToggle(at, "Baim in Air", 622, false, function(s) Settings.BaimInAir = s end)
at.CanvasSize = UDim2.new(0, 0, 0, 670)

-- Tab 2: CROSSHAIR
local ch = TabPages[2]
CreateSection(ch, "✦ CROSSHAIR STYLE", 5)
CreateDropdown(ch, "Style", 38, {"Heart","Square","Hexagon","Star","Cross"}, "Heart", function(v) Settings.CrosshairStyle = v end)
CreateToggle(ch, "Show Crosshair", 74, true, function(s) Settings.ShowCrosshair = s end)
CreateToggle(ch, "Rotate Animation", 110, true, function(s) Settings.CrosshairRotation = s end)
CreateSection(ch, "🎨 APPEARANCE", 152)
CreateSlider(ch, "Size (%)", 185, 20, 300, 100, function(v) Settings.CrosshairSize = v end)
CreateSlider(ch, "Thickness (%)", 237, 20, 400, 100, function(v) Settings.CrosshairThickness = v end)
CreateSlider(ch, "Opacity (%)", 289, 0, 100, 100, function(v) Settings.CrosshairOpacity = v end)
CreateColorPicker(ch, "Crosshair Color", 341, Settings.CrosshairColor, function(v) Settings.CrosshairColor = v end)
ch.CanvasSize = UDim2.new(0, 0, 0, 390)

-- Tab 3: ANTI-AIM
local aa = TabPages[3]
CreateSection(aa, "🔄 CORE", 5)
CreateToggle(aa, "Anti-Aim Enabled", 38, false, function(s)
    Settings.AntiAimEnabled = s
    if not s then
        local ch2 = LocalPlayer.Character
        if ch2 then local h = ch2:FindFirstChildOfClass("Humanoid"); if h then h.AutoRotate = true end end
    end
end)
CreateSection(aa, "🌀 YAW", 80)
CreateDropdown(aa, "Yaw Mode", 113, {"Spin","Backwards","Jitter","Random","Custom","None"}, "Spin", function(v) Settings.YawMode = v end)
CreateSlider(aa, "Spin Speed", 147, 1, 30, 5, function(v) Settings.SpinSpeed = v end)
CreateSlider(aa, "Yaw Offset (°)", 199, -180, 180, 0, function(v) Settings.YawOffset = v end)
CreateSlider(aa, "Jitter Range (°)", 251, 0, 180, 45, function(v) Settings.YawJitterRange = v end)
CreateSection(aa, "📐 PITCH & EXTRAS", 303)
CreateDropdown(aa, "Pitch Mode", 336, {"None","Down","Up","Zero","Fake Down"}, "None", function(v) Settings.PitchMode = v end)
CreateSlider(aa, "Roll (°)", 378, -50, 50, 0, function(v) Settings.RollAngle = v end)
CreateDropdown(aa, "Manual AA", 420, {"None","Left","Right","Back","Front"}, "None", function(v) Settings.ManualAA = v end)
CreateToggle(aa, "Inverter", 458, false, function(s) Settings.InverterActive = s end)
CreateToggle(aa, "Freestanding", 494, false, function(s) Settings.Freestanding = s end)
aa.CanvasSize = UDim2.new(0, 0, 0, 540)

-- Tab 4: ESP
local et = TabPages[4]
CreateSection(et, "👁 ESP MAIN", 5)
CreateToggle(et, "ESP Enabled", 38, false, function(s) Settings.ESPEnabled = s end)
CreateDropdown(et, "Box Style", 74, {"2D","3D","Corner"}, "2D", function(v) Settings.ESPBoxStyle = v end)
CreateSection(et, "👁 FEATURES", 116)
CreateToggle(et, "Boxes", 149, true, function(s) Settings.ESPBoxes = s end)
CreateToggle(et, "Names", 185, true, function(s) Settings.ESPNames = s end)
CreateToggle(et, "Health Bar", 221, true, function(s) Settings.ESPHealth = s end)
CreateToggle(et, "Distance", 257, false, function(s) Settings.ESPDistance = s end)
CreateToggle(et, "Weapon ESP", 293, false, function(s) Settings.ESPWeapon = s end)
CreateToggle(et, "Skeleton", 329, false, function(s) Settings.ESPSkeleton = s end)
CreateToggle(et, "Snap Lines", 365, false, function(s) Settings.ESPSnapLines = s end)
CreateToggle(et, "Tracers", 401, false, function(s) Settings.ESPTracers = s end)
CreateToggle(et, "Dormant ESP", 437, false, function(s) Settings.ESPDormant = s end)
CreateSection(et, "🎨 APPEARANCE", 479)
CreateSlider(et, "ESP Size (%)", 512, 50, 200, 100, function(v) Settings.ESPSize = v end)
CreateSlider(et, "ESP Opacity (%)", 564, 0, 100, 100, function(v) Settings.ESPOpacity = v end)
CreateSection(et, "✨ CHAMS & GLOW", 616)
CreateToggle(et, "Chams Enabled", 649, false, function(s) Settings.ChamsEnabled = s; RefreshChams() end)
CreateDropdown(et, "Chams Mode", 685, {"Visible","Invisible","Wall"}, "Visible", function(v) Settings.ChamsMode = v; RefreshChams() end)
CreateToggle(et, "Glow Effect", 721, false, function(s) Settings.GlowEnabled = s; RefreshChams() end)
CreateSection(et, "📡 RADAR", 763)
CreateToggle(et, "Radar Enabled", 796, false, function(s) Settings.RadarEnabled = s; RadarFrame.Visible = s end)
CreateSlider(et, "Radar Zoom", 832, 20, 200, 50, function(v) Settings.RadarZoom = v end)
et.CanvasSize = UDim2.new(0, 0, 0, 900)

-- Tab 5: VISUALS
local vt = TabPages[5]
CreateSection(vt, "🎥 CAMERA", 5)
CreateSlider(vt, "Camera FOV", 38, 30, 120, 70, function(v) Settings.CameraFOV = v; ApplyWorldSettings() end)
CreateToggle(vt, "Third Person", 90, false, function(s) Settings.ThirdPerson = s; ApplyWorldSettings() end)
CreateSlider(vt, "Third Person Zoom", 126, 5, 40, 15, function(v) Settings.ThirdPersonZoom = v; ApplyWorldSettings() end)
CreateSection(vt, "🌟 VISUALS", 178)
CreateToggle(vt, "Full Bright", 211, false, function(s) Settings.FullBright = s; if s then Settings.NightMode = false end; ApplyWorldSettings() end)
CreateToggle(vt, "Night Mode", 247, false, function(s) Settings.NightMode = s; if s then Settings.FullBright = false end; ApplyWorldSettings() end)
CreateToggle(vt, "Remove Visual Recoil", 283, false, function(s) Settings.RemoveVisualRecoil = s end)
vt.CanvasSize = UDim2.new(0, 0, 0, 330)

-- Tab 6: WORLD
local wt = TabPages[6]
CreateSection(wt, "🌅 LIGHTING", 5)
CreateColorPicker(wt, "Sky Color", 38, Settings.WorldSkyColor, function(v) Settings.WorldSkyColor = v; ApplyWorldSettings() end)
CreateColorPicker(wt, "Ambient", 74, Settings.WorldAmbient, function(v) Settings.WorldAmbient = v; ApplyWorldSettings() end)
CreateSlider(wt, "Brightness", 110, 0, 5, 1, function(v) Settings.WorldBrightness = v; ApplyWorldSettings() end)
CreateSection(wt, "🌫 FOG", 162)
CreateToggle(wt, "Fog Enabled", 195, false, function(s) Settings.WorldFog = s; ApplyWorldSettings() end)
CreateColorPicker(wt, "Fog Color", 231, Settings.WorldFogColor, function(v) Settings.WorldFogColor = v; ApplyWorldSettings() end)
CreateSlider(wt, "Fog Start", 267, 0, 1000, 0, function(v) Settings.WorldFogStart = v; ApplyWorldSettings() end)
CreateSlider(wt, "Fog End", 319, 100, 5000, 1000, function(v) Settings.WorldFogEnd = v; ApplyWorldSettings() end)
CreateButton(wt, "🔄 Reset World", 371, function() ResetWorld(); Notify("✅ World Reset!") end)
wt.CanvasSize = UDim2.new(0, 0, 0, 420)

-- Tab 7: WEATHER (No GUI Particles!)
local wet = TabPages[7]
CreateSection(wet, "⛅ WEATHER", 5)
CreateToggle(wet, "Weather Enabled", 38, false, function(s) Settings.WeatherEnabled = s; if s then InitWeather() else ClearWeather() end end)
CreateDropdown(wet, "Weather Type", 74, {"None","Snow","Rain","Lightning"}, "None", function(v) Settings.WeatherType = v; if Settings.WeatherEnabled then InitWeather() end end)
CreateSlider(wet, "Intensity", 110, 10, 200, 50, function(v) Settings.WeatherIntensity = v; if Settings.WeatherEnabled then InitWeather() end end)
CreateLabel(wet, "Note: Weather particles are in-game only", 5, 165, 400, 20, 11, Color3.fromRGB(150, 150, 180))
wet.CanvasSize = UDim2.new(0, 0, 0, 200)

-- Tab 8: MOVEMENT
local mt = TabPages[8]
CreateSection(mt, "🏃 MOVEMENT HACKS", 5)
CreateToggle(mt, "Wall Jump", 38, false, function(s) Settings.WallJump = s end)
CreateToggle(mt, "Bunny Hop", 74, false, function(s) Settings.BunnyHop = s end)
CreateToggle(mt, "Infinity Jump", 110, false, function(s) Settings.InfinityJump = s end)
CreateToggle(mt, "Jump Bug", 146, false, function(s) Settings.JumpBug = s end)
CreateToggle(mt, "Circle Strafe", 182, false, function(s) Settings.CircleStrafe = s end)
CreateToggle(mt, "Strafe in Air", 218, false, function(s) Settings.StrafeInAir = s end)
CreateToggle(mt, "Quick Stop", 254, false, function(s) Settings.QuickStop = s end)
CreateSection(mt, "⚡ SPEED", 296)
CreateToggle(mt, "Walk Speed Boost", 329, false, function(s)
    Settings.WalkSpeedBoost = s
    local ch2 = LocalPlayer.Character
    if ch2 then local hu = ch2:FindFirstChildOfClass("Humanoid"); if hu then hu.WalkSpeed = s and Settings.WalkSpeedValue or 16 end end
end)
CreateSlider(mt, "Walk Speed", 365, 16, 200, 16, function(v)
    Settings.WalkSpeedValue = v
    if Settings.WalkSpeedBoost then
        local ch2 = LocalPlayer.Character
        if ch2 then local hu = ch2:FindFirstChildOfClass("Humanoid"); if hu then hu.WalkSpeed = v end end
    end
end)
CreateToggle(mt, "Jump Power Boost", 417, false, function(s)
    Settings.JumpPowerBoost = s
    local ch2 = LocalPlayer.Character
    if ch2 then local hu = ch2:FindFirstChildOfClass("Humanoid"); if hu then hu.JumpPower = s and Settings.JumpPowerValue or 50 end end
end)
CreateSlider(mt, "Jump Power", 453, 50, 300, 50, function(v)
    Settings.JumpPowerValue = v
    if Settings.JumpPowerBoost then
        local ch2 = LocalPlayer.Character
        if ch2 then local hu = ch2:FindFirstChildOfClass("Humanoid"); if hu then hu.JumpPower = v end end
    end
end)
CreateSection(mt, "🛡 BYPASS (SAFE)", 505)
CreateToggle(mt, "Bypass Movement (No Hooks)", 538, false, function(s)
    Settings.BypassMovement = s
    if s then Notify("✅ Safe Bypass Enabled!") else Notify("❌ Disabled") end
end)
mt.CanvasSize = UDim2.new(0, 0, 0, 600)

-- Tab 9: EXTRA
local ext = TabPages[9]
CreateSection(ext, "🛠 EXTRA FEATURES", 5)
CreateToggle(ext, "Disable Console", 38, false, function(s) Settings.DisableConsole = s; if s then pcall(function() for _, gui in ipairs(CoreGui:GetChildren()) do if gui.Name == "DevConsoleMaster" then gui:Destroy() end end end) end end)
CreateToggle(ext, "Disable Server Ads", 74, false, function(s) Settings.DisableServerAds = s end)
CreateToggle(ext, "Show FPS", 110, false, function(s) Settings.ShowFPS = s; StatsHUD.Visible = s or Settings.ShowMS or Settings.FakePing end)
CreateToggle(ext, "Fake Ping", 146, false, function(s) Settings.FakePing = s; StatsHUD.Visible = Settings.ShowFPS or Settings.ShowMS or s end)
CreateSlider(ext, "Fake Ping Value", 184, 1, 9999, 999, function(v) Settings.FakePingValue = v end)
CreateToggle(ext, "Show MS", 236, false, function(s) Settings.ShowMS = s; StatsHUD.Visible = Settings.ShowFPS or s or Settings.FakePing end)
CreateToggle(ext, "Show Player FPS (ESP)", 272, false, function(s) Settings.ShowPlayerFPS = s end)
CreateToggle(ext, "Show Player MS (ESP)", 308, false, function(s) Settings.ShowPlayerMS = s end)
CreateToggle(ext, "Auto Peek", 344, false, function(s) Settings.AutoPeek = s; AutoPeekIndicator.Visible = s end)
ext.CanvasSize = UDim2.new(0, 0, 0, 400)

-- Tab 10: INVENTORY
local inv = TabPages[10]
CreateSection(inv, "🎒 TOOL ESP", 5)
CreateToggle(inv, "Tool ESP", 38, false, function(s) Settings.ToolESP = s end)
CreateColorPicker(inv, "Tool ESP Color", 74, Settings.ToolESPColor, function(v) Settings.ToolESPColor = v end)
CreateToggle(inv, "Show Tool Name", 110, false, function(s) Settings.ToolESPName = s end)
CreateColorPicker(inv, "Tool Name Color", 146, Settings.ToolESPNameColor, function(v) Settings.ToolESPNameColor = v end)
CreateSection(inv, "🗑 INVENTORY ACTIONS", 188)
CreateButton(inv, "🗑 Remove All Items", 221, function()
    local ch2 = LocalPlayer.Character
    local bp = LocalPlayer:FindFirstChild("Backpack")
    local removed = 0
    if ch2 then for _, t in ipairs(ch2:GetChildren()) do if t:IsA("Tool") then t:Destroy(); removed = removed + 1 end end end
    if bp then for _, t in ipairs(bp:GetChildren()) do if t:IsA("Tool") then t:Destroy(); removed = removed + 1 end end end
    Notify("🗑 Removed "..removed.." items!")
end)
CreateButton(inv, "📋 List Inventory", 263, function()
    local bp = LocalPlayer:FindFirstChild("Backpack")
    if bp then local count = 0; for _, t in ipairs(bp:GetChildren()) do count = count + 1 end Notify("🎒 Items: "..count) end
end)
inv.CanvasSize = UDim2.new(0, 0, 0, 320)

-- Tab 11: INDICATOR
local ind = TabPages[11]
CreateSection(ind, "📊 INDICATORS", 5)
CreateToggle(ind, "Teleport Cooldown", 38, false, function(s) Settings.TeleportCooldownInd = s; TPCooldownFrame.Visible = s end)
CreateToggle(ind, "Inventory Full Warning", 74, false, function(s) Settings.InventoryFullWarning = s end)
CreateToggle(ind, "Compass", 110, false, function(s) Settings.Compass = s; CompassFrame.Visible = s end)
CreateToggle(ind, "Nearby Loot", 146, false, function(s) Settings.NearbyLoot = s end)
CreateSection(ind, "💥 DAMAGE TEXT", 188)
CreateToggle(ind, "Damage Text", 221, false, function(s) Settings.DamageText = s end)
CreateColorPicker(ind, "Damage Text Color", 257, Settings.DamageTextColor, function(v) Settings.DamageTextColor = v end)
CreateSection(ind, "⚠ NEARBY ENEMY", 299)
CreateToggle(ind, "Nearby Enemy Alert", 332, false, function(s) Settings.NearbyEnemy = s; if not s then NearbyAlert.Visible = false end end)
CreateColorPicker(ind, "Alert Color", 368, Settings.NearbyEnemyColor, function(v) Settings.NearbyEnemyColor = v; NearbyStroke.Color = C3(v); NearbyLabel.TextColor3 = C3(v) end)
ind.CanvasSize = UDim2.new(0, 0, 0, 420)

-- Tab 12: COLORS
local ct = TabPages[12]
CreateSection(ct, "🎨 COLORS", 5)
CreateColorPicker(ct, "FOV Circle", 38, Settings.FOVColor, function(v) Settings.FOVColor = v end)
CreateColorPicker(ct, "ESP Color", 74, Settings.ESPColor, function(v) Settings.ESPColor = v end)
CreateColorPicker(ct, "Crosshair", 110, Settings.CrosshairColor, function(v) Settings.CrosshairColor = v end)
CreateColorPicker(ct, "Chams Fill", 146, Settings.ChamsColor, function(v) Settings.ChamsColor = v; UpdateChamsColor() end)
CreateColorPicker(ct, "Chams Outline", 182, Settings.ChamsOutlineColor, function(v) Settings.ChamsOutlineColor = v; UpdateChamsColor() end)
CreateColorPicker(ct, "Glow Color", 218, Settings.GlowColor, function(v) Settings.GlowColor = v; UpdateChamsColor() end)
CreateColorPicker(ct, "Skeleton Color", 254, Settings.SkeletonColor, function(v) Settings.SkeletonColor = v end)
CreateColorPicker(ct, "Accent Color", 290, Settings.AccentColor, function(v) Settings.AccentColor = v; MainStroke.Color = C3(v); ToggleStroke.Color = C3(v) end)
CreateButton(ct, "🔄 Reset Colors", 332, function()
    Settings.FOVColor = {180,100,255}; Settings.ESPColor = {180,100,255}
    Settings.CrosshairColor = {255,80,180}; Settings.ChamsColor = {180,50,255}
    Settings.ChamsOutlineColor = {255,255,255}; Settings.GlowColor = {180,100,255}
    Settings.SkeletonColor = {255,255,255}; Settings.AccentColor = {180,100,255}
    UpdateChamsColor(); Notify("✅ Colors Reset!")
end)
ct.CanvasSize = UDim2.new(0, 0, 0, 380)

-- Tab 13: SETTINGS
local st = TabPages[13]
CreateSection(st, "🔒 TOGGLE BUTTON", 5)
CreateToggle(st, "Lock Toggle Button", 38, false, function(s) Settings.ToggleButtonLocked = s; LockIcon.Visible = s; if s then Notify("🔒 Toggle locked!") else Notify("🔓 Toggle unlocked!") end end)
CreateSection(st, "📐 GUI SIZE", 80)
CreateSlider(st, "GUI Scale (%)", 113, 50, 150, 100, function(v)
    Settings.GUIScale = v
    ApplyGUIScale()
end)
CreateLabel(st, "Adjust the size of the main GUI", 5, 170, 400, 20, 11, Color3.fromRGB(150, 150, 180))
CreateLabel(st, "Toggle button also scales (50%-150%)", 5, 190, 400, 20, 11, Color3.fromRGB(150, 150, 180))
CreateSection(st, "💾 CONFIG SYSTEM (5 SLOTS)", 220)
local configSlotLabel = CreateLabel(st, "Current: " .. CurrentConfigSlot, 5, 252, 300, 20, 13, Color3.fromRGB(180, 220, 255))
CreateDropdown(st, "Config Slot", 278, ConfigSlots, "Slot1", function(v)
    CurrentConfigSlot = v
    configSlotLabel.Text = "Current: " .. v
end)
CreateButton(st, "💾 SAVE Config", 314, function()
    local ok, err = SaveConfig(CurrentConfigSlot)
    if ok then Notify("✅ Saved to " .. CurrentConfigSlot .. "!")
    else Notify("❌ Save failed") end
end)
CreateButton(st, "📂 LOAD Config", 356, function()
    if LoadConfig(CurrentConfigSlot) then Notify("✅ Loaded " .. CurrentConfigSlot .. "!")
    else Notify("❌ Failed to load!") end
end)
CreateButton(st, "🗑 DELETE Config", 398, function()
    DeleteConfig(CurrentConfigSlot)
    Notify("🗑 Deleted " .. CurrentConfigSlot)
end)
CreateButton(st, "🗑 Reset All Settings", 440, function()
    Settings.AimbotEnabled = false; Settings.ESPEnabled = false
    Settings.ChamsEnabled = false; Settings.AntiAimEnabled = false
    Settings.GlowEnabled = false; Settings.WeatherEnabled = false
    ClearWeather(); RefreshChams(); ResetWorld()
    Notify("✅ Reset Complete!")
end)
CreateSection(st, "ℹ INFO", 488)
CreateLabel(st, "AIMBOT PRO v13.2 - SAFE", 5, 520, 400, 20, 13, Color3.fromRGB(255, 220, 255))
CreateLabel(st, "✅ No detection (no hooks used)", 5, 545, 400, 20, 11, Color3.fromRGB(80, 255, 120))
CreateLabel(st, "✦ 5 Crosshairs (Heart/Square/Hex/Star/Cross)", 5, 565, 400, 20, 11)
CreateLabel(st, "✦ GUI Scale adjustment", 5, 585, 400, 20, 11)
CreateLabel(st, "✦ Loading screen + Clean UI", 5, 605, 400, 20, 11)
CreateLabel(st, "✦ 5-Slot Config System", 5, 625, 400, 20, 11)
st.CanvasSize = UDim2.new(0, 0, 0, 650)

-- ========== INPUT ==========
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if not Settings.AimbotEnabled then return end
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        if Settings.AimbotMode == "Hold" then AimbotActive = true
        elseif Settings.AimbotMode == "Toggle" then ToggleActive = not ToggleActive end
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if Settings.AimbotMode == "Hold" and input.UserInputType == Enum.UserInputType.MouseButton2 then
        AimbotActive = false
    end
end)

UserInputService.JumpRequest:Connect(function()
    if Settings.BunnyHop then
        local ch2 = LocalPlayer.Character
        if ch2 then local hu = ch2:FindFirstChildOfClass("Humanoid"); if hu then hu.Jump = true end end
    end
end)

local SkeletonBones = {
    {"Head", "UpperTorso"}, {"UpperTorso", "LowerTorso"},
    {"UpperTorso", "LeftUpperArm"}, {"UpperTorso", "RightUpperArm"},
    {"LowerTorso", "LeftUpperLeg"}, {"LowerTorso", "RightUpperLeg"},
}

local fpsFrames = 0
local fpsTime = 0
local currentFPS = 0
local toolESPTimer = 0
local ToolESPData = {}

local function UpdateToolESP()
    if not Settings.ToolESP then
        for _, data in pairs(ToolESPData) do if data.hl then pcall(function() data.hl:Destroy() end) end; if data.bb then pcall(function() data.bb:Destroy() end) end end
        ToolESPData = {}
        return
    end
    local foundTools = {}
    for _, obj in ipairs(workspace:GetDescendants()) do
        if obj:IsA("Tool") then
            local par = obj.Parent
            local pChar = par and Players:GetPlayerFromCharacter(par)
            if par and not pChar then
                foundTools[obj] = true
                if not ToolESPData[obj] then
                    ToolESPData[obj] = {}
                    local handle = obj:FindFirstChild("Handle") or obj:FindFirstChildWhichIsA("BasePart")
                    if handle then
                        local hl = Instance.new("Highlight")
                        hl.FillColor = C3(Settings.ToolESPColor)
                        hl.OutlineColor = C3(Settings.ToolESPColor)
                        hl.FillTransparency = 0.5
                        hl.OutlineTransparency = 0
                        hl.Adornee = obj
                        pcall(function() hl.Parent = obj end)
                        ToolESPData[obj].hl = hl
                        if Settings.ToolESPName then
                            local bb = Instance.new("BillboardGui")
                            bb.Size = UDim2.new(0, 100, 0, 30)
                            bb.StudsOffset = Vector3.new(0, 2, 0)
                            bb.AlwaysOnTop = true
                            bb.Adornee = handle
                            local lbl = Instance.new("TextLabel")
                            lbl.Size = UDim2.new(1, 0, 1, 0)
                            lbl.BackgroundTransparency = 1
                            lbl.Text = obj.Name
                            lbl.TextColor3 = C3(Settings.ToolESPNameColor)
                            lbl.TextSize = 12
                            lbl.Font = Enum.Font.GothamBold
                            lbl.TextStrokeTransparency = 0
                            lbl.Parent = bb
                            pcall(function() bb.Parent = handle end)
                            ToolESPData[obj].bb = bb
                        end
                    end
                else
                    if ToolESPData[obj].hl then ToolESPData[obj].hl.FillColor = C3(Settings.ToolESPColor); ToolESPData[obj].hl.OutlineColor = C3(Settings.ToolESPColor) end
                end
            end
        end
    end
    for tool, data in pairs(ToolESPData) do
        if not foundTools[tool] or not tool.Parent then
            if data.hl then pcall(function() data.hl:Destroy() end) end
            if data.bb then pcall(function() data.bb:Destroy() end) end
            ToolESPData[tool] = nil
        end
    end
end

-- ========== MAIN LOOP ==========
local heartRot = 0
local tt = 0
local dormantPositions = {}

RunService.RenderStepped:Connect(function(dt)
    tt = tt + dt
    local center = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)

    ApplyAntiAim(dt)
    SafeBypassUpdate()

    fpsFrames = fpsFrames + 1
    fpsTime = fpsTime + dt
    if fpsTime >= 0.5 then
        currentFPS = math.floor(fpsFrames / fpsTime)
        fpsFrames = 0; fpsTime = 0
    end

    if Settings.ShowFPS then FPSLabel.Text = "FPS: " .. currentFPS; FPSLabel.Visible = true
    else FPSLabel.Visible = false end
    if Settings.ShowMS then MSLabel.Text = "MS: " .. math.floor(dt*1000); MSLabel.Visible = true
    else MSLabel.Visible = false end
    if Settings.FakePing then PingLabel.Text = "Ping: " .. Settings.FakePingValue .. "ms"; PingLabel.Visible = true
    else
        local ok, ping = pcall(function() return math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue()) end)
        if ok then PingLabel.Text = "Ping: " .. ping .. "ms"; PingLabel.Visible = true else PingLabel.Visible = false end
    end

    -- Movement
    local ch2 = LocalPlayer.Character
    if ch2 then
        local hu = ch2:FindFirstChildOfClass("Humanoid")
        local hrp = ch2:FindFirstChild("HumanoidRootPart")
        if hu and hrp then
            if Settings.BunnyHop and hu:GetState() == Enum.HumanoidStateType.Landed then hu.Jump = true end
            if Settings.CircleStrafe then
                local moveDir = hu.MoveDirection
                if moveDir.Magnitude > 0 then
                    local angle = tt * 3
                    hrp.CFrame = CFrame.new(hrp.Position) * CFrame.Angles(0, angle, 0)
                end
            end
            if Settings.StrafeInAir and hu:GetState() == Enum.HumanoidStateType.Freefall then
                local moveDir = hu.MoveDirection
                if moveDir.Magnitude > 0 then hrp.Velocity = Vector3.new(moveDir.X * 30, hrp.Velocity.Y, moveDir.Z * 30) end
            end
            if Settings.QuickStop and hu.MoveDirection.Magnitude < 0.1 then hrp.Velocity = Vector3.new(0, hrp.Velocity.Y, 0) end
            if Settings.JumpBug and hu:GetState() == Enum.HumanoidStateType.Freefall and hrp.Velocity.Y < -30 then
                hu:ChangeState(Enum.HumanoidStateType.Landed)
            end
        end
    end

    if Settings.Compass and ch2 then
        local dir = Camera.CFrame.LookVector
        local ang = math.deg(math.atan2(-dir.X, -dir.Z))
        local dirName = "N"
        if ang > -22.5 and ang <= 22.5 then dirName = "N"
        elseif ang > 22.5 and ang <= 67.5 then dirName = "NE"
        elseif ang > 67.5 and ang <= 112.5 then dirName = "E"
        elseif ang > 112.5 and ang <= 157.5 then dirName = "SE"
        elseif ang > 157.5 or ang <= -157.5 then dirName = "S"
        elseif ang > -157.5 and ang <= -112.5 then dirName = "SW"
        elseif ang > -112.5 and ang <= -67.5 then dirName = "W"
        elseif ang > -67.5 and ang <= -22.5 then dirName = "NW" end
        CompassLabel.Text = dirName .. " (" .. math.floor(ang) .. "°)"
    end

    if Settings.InventoryFullWarning then
        local bp = LocalPlayer:FindFirstChild("Backpack")
        if bp then
            local count = 0
            for _, t in ipairs(bp:GetChildren()) do if t:IsA("Tool") then count = count + 1 end end
            if count >= 6 then InvFullFrame.Visible = true; InvFullLabel.Text = "⚠ Inventory Full ("..count..")"
            else InvFullFrame.Visible = false end
        end
    else InvFullFrame.Visible = false end

    if Settings.TeleportCooldownInd then
        TPCooldownFrame.Visible = true
        TPCooldownLabel.Text = "TP: Ready"
        TPCooldownLabel.TextColor3 = Color3.fromRGB(80, 255, 120)
    end

    if Settings.NearbyEnemy and ch2 then
        local hrp = ch2:FindFirstChild("HumanoidRootPart")
        if hrp then
            local nearby = false; local closestDist = math.huge
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                    local d = (p.Character.HumanoidRootPart.Position - hrp.Position).Magnitude
                    if d < 30 then nearby = true; if d < closestDist then closestDist = d end end
                end
            end
            NearbyAlert.Visible = nearby
            if nearby then
                NearbyLabel.Text = "⚠ ENEMY " .. math.floor(closestDist) .. "m"
                NearbyStroke.Color = C3(Settings.NearbyEnemyColor)
                NearbyLabel.TextColor3 = C3(Settings.NearbyEnemyColor)
            end
        end
    end

    toolESPTimer = toolESPTimer + dt
    if toolESPTimer > 0.5 then toolESPTimer = 0; UpdateToolESP() end

    if Settings.WeatherEnabled and #WeatherParticles > 0 then
        for _, p in ipairs(WeatherParticles) do
            local pos = p.frame.Position
            local nx = pos.X.Scale + p.vx * dt
            local ny = pos.Y.Scale + p.vy * dt
            if ny > 1.1 then ny = -0.1; nx = math.random() end
            if nx > 1.1 then nx = 0 elseif nx < -0.1 then nx = 1 end
            p.frame.Position = UDim2.new(nx, 0, ny, 0)
        end
    end

    if FOVCircle then
        FOVCircle.Position = center
        FOVCircle.Radius = Settings.FOV
        FOVCircle.Color = C3(Settings.FOVColor)
        FOVCircle.Thickness = Settings.FOVThickness
        FOVCircle.Visible = Settings.ShowFOV
    end

    -- CROSSHAIR
    local sizeMult = Settings.CrosshairSize / 100
    local thickness = Settings.CrosshairThickness / 100
    local opacity = Settings.CrosshairOpacity / 100
    local showCh = Settings.ShowCrosshair and opacity > 0.01

    for _, d in ipairs(HeartDots) do d.Visible = false end
    for _, l in ipairs(SquareLines) do l.Visible = false end
    for _, l in ipairs(HexagonLines) do l.Visible = false end
    for _, l in ipairs(StarLines) do l.Visible = false end

    if showCh then
        if Settings.CrosshairStyle == "Heart" then
            local rot = Settings.CrosshairRotation and math.rad(heartRot) or 0
            for i, dot in ipairs(HeartDots) do
                local t = (i-1) * 0.2
                local x = 14 * (math.sin(t)^3)
                local y = -(11*math.cos(t) - 4*math.cos(2*t) - 2*math.cos(3*t) - math.cos(4*t))
                local scaled = Vector2.new(x * sizeMult, y * sizeMult)
                local rv = RotateV2(scaled, rot)
                dot.Position = center + rv
                dot.Radius = 1.6 * thickness
                dot.Color = C3(Settings.CrosshairColor)
                dot.Transparency = opacity
                dot.Visible = true
            end
            heartRot = (heartRot + dt * 85) % 360
        elseif Settings.CrosshairStyle == "Square" then
            local sz = 12 * sizeMult
            local pts = {
                Vector2.new(-sz, -sz), Vector2.new(sz, -sz),
                Vector2.new(sz, -sz), Vector2.new(sz, sz),
                Vector2.new(sz, sz), Vector2.new(-sz, sz),
                Vector2.new(-sz, sz), Vector2.new(-sz, -sz),
                Vector2.new(0,0), Vector2.new(0,0)
            }
            for i = 1, 5 do
                SquareLines[i].From = center + pts[(i-1)*2 + 1]
                SquareLines[i].To = center + pts[(i-1)*2 + 2]
                SquareLines[i].Color = C3(Settings.CrosshairColor)
                SquareLines[i].Thickness = 1.5 * thickness
                SquareLines[i].Transparency = opacity
                SquareLines[i].Visible = true
            end
        elseif Settings.CrosshairStyle == "Hexagon" then
            local sz = 12 * sizeMult
            for i = 1, 7 do
                local a1 = (i-1) * (math.pi*2/6) + (Settings.CrosshairRotation and tt * 0.5 or 0)
                local a2 = i * (math.pi*2/6) + (Settings.CrosshairRotation and tt * 0.5 or 0)
                HexagonLines[i].From = center + Vector2.new(math.cos(a1)*sz, math.sin(a1)*sz)
                HexagonLines[i].To = center + Vector2.new(math.cos(a2)*sz, math.sin(a2)*sz)
                HexagonLines[i].Color = C3(Settings.CrosshairColor)
                HexagonLines[i].Thickness = 1.5 * thickness
                HexagonLines[i].Transparency = opacity
                HexagonLines[i].Visible = true
            end
        elseif Settings.CrosshairStyle == "Star" then
            local outerR = 14 * sizeMult
            local innerR = 6 * sizeMult
            local rotOffset = Settings.CrosshairRotation and tt * 0.8 or 0
            for i = 1, 11 do
                local a1 = (i-1) * (math.pi*2/10) + rotOffset
                local a2 = i * (math.pi*2/10) + rotOffset
                local r1 = (i % 2 == 0) and innerR or outerR
                local r2 = (i % 2 == 0) and outerR or innerR
                StarLines[i].From = center + Vector2.new(math.cos(a1)*r1, math.sin(a1)*r1)
                StarLines[i].To = center + Vector2.new(math.cos(a2)*r2, math.sin(a2)*r2)
                StarLines[i].Color = C3(Settings.CrosshairColor)
                StarLines[i].Thickness = 1.5 * thickness
                StarLines[i].Transparency = opacity
                StarLines[i].Visible = true
            end
        elseif Settings.CrosshairStyle == "Cross" then
            local sz = 14 * sizeMult
            local crossPts = {Vector2.new(0, -sz), Vector2.new(0, 0), Vector2.new(-sz, 0), Vector2.new(0, 0)}
            for i = 1, 4 do
                SquareLines[i].From = center + crossPts[i]
                SquareLines[i].To = center + Vector2.new(0, 0)
            end
            for i = 1, 4 do
                SquareLines[i].Color = C3(Settings.CrosshairColor)
                SquareLines[i].Thickness = 1.5 * thickness
                SquareLines[i].Transparency = opacity
                SquareLines[i].Visible = true
            end
        end
    end

    -- Aimbot
    local shouldAim = false
    if Settings.AimbotEnabled then
        if Settings.AimbotMode == "Always" then shouldAim = true
        elseif Settings.AimbotMode == "Hold" then shouldAim = AimbotActive
        elseif Settings.AimbotMode == "Toggle" then shouldAim = ToggleActive end
    end
    if shouldAim then
        local target, pos = GetClosestPlayer()
        if target and pos then
            local tcf = CFrame.new(Camera.CFrame.Position, pos)
            if Settings.Smoothness <= 0 then Camera.CFrame = tcf
            else
                local a = math.clamp(dt * (22 - Settings.Smoothness) * 3, 0, 1)
                Camera.CFrame = Camera.CFrame:Lerp(tcf, a)
            end
        end
    end

    -- Radar
    if Settings.RadarEnabled and RadarFrame.Visible then
        local lp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        for p, dot in pairs(RadarDots) do
            if not p.Parent or not p.Character then dot.Visible = false
            else
                local root = p.Character:FindFirstChild("HumanoidRootPart")
                if root and lp then
                    local rel = root.Position - lp.Position
                    local camCF = Camera.CFrame
                    local look = Vector3.new(camCF.LookVector.X, 0, camCF.LookVector.Z).Unit
                    local ang = math.atan2(look.X, look.Z)
                    local cs, sn = math.cos(-ang), math.sin(-ang)
                    local rx = rel.X * cs - rel.Z * sn
                    local rz = rel.X * sn + rel.Z * cs
                    local zoom = Settings.RadarZoom
                    local px = math.clamp(0.5 + (rx / zoom / 2), 0, 1)
                    local py = math.clamp(0.5 + (rz / zoom / 2), 0, 1)
                    dot.Position = UDim2.new(px, -3, py, -3)
                    dot.BackgroundColor3 = C3(Settings.ESPColor)
                    dot.Visible = true
                else dot.Visible = false end
            end
        end
        for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then GetRadarDot(p) end end
    else
        for _, dot in pairs(RadarDots) do dot.Visible = false end
    end

    -- ESP
    local espSizeMult = Settings.ESPSize / 100
    local espOpacity = Settings.ESPOpacity / 100
    for player, esp in pairs(ESPData) do
        if not player or not player.Parent then HideESP(esp); continue end
        if not Settings.ESPEnabled then HideESP(esp); continue end
        local ch3 = player.Character
        if not ch3 then
            if Settings.ESPDormant and dormantPositions[player] and esp.DormantMarker then
                local dp = dormantPositions[player]
                local sp, os = Camera:WorldToViewportPoint(dp)
                if os then
                    esp.DormantMarker.From = Vector2.new(sp.X, sp.Y - 10)
                    esp.DormantMarker.To = Vector2.new(sp.X, sp.Y + 10)
                    esp.DormantMarker.Color = Color3.fromRGB(150, 150, 150)
                    esp.DormantMarker.Transparency = 1 - espOpacity
                    esp.DormantMarker.Visible = true
                end
            else HideESP(esp) end
            continue
        end
        local hu = ch3:FindFirstChildOfClass("Humanoid")
        local root = ch3:FindFirstChild("HumanoidRootPart")
        local head = ch3:FindFirstChild("Head")
        if not (hu and hu.Health > 0 and root and head) then HideESP(esp); continue end
        dormantPositions[player] = root.Position
        local ht = head.Position + Vector3.new(0, head.Size.Y/2, 0)
        local rb = root.Position - Vector3.new(0, root.Size.Y/2 + 2.3, 0)
        local hs, hv = Camera:WorldToViewportPoint(ht)
        local rs, rv = Camera:WorldToViewportPoint(rb)
        if not (hv and rv) then HideESP(esp); continue end
        local top, bot = hs.Y, rs.Y
        local h = bot - top
        local cx = (hs.X + rs.X) / 2
        local w = math.max(h * 0.5, 20) * espSizeMult

        for _, l in ipairs(esp.BoxLines) do l.Visible = false end
        for _, l in ipairs(esp.CornerLines) do l.Visible = false end
        for _, l in ipairs(esp.Box3DLines) do l.Visible = false end

        if Settings.ESPBoxes then
            if Settings.ESPBoxStyle == "2D" then
                local tl = Vector2.new(cx - w/2, top)
                local tr2 = Vector2.new(cx + w/2, top)
                local bl = Vector2.new(cx - w/2, bot)
                local br = Vector2.new(cx + w/2, bot)
                local pts = {tl, tr2, br, bl}
                for i = 1, 4 do
                    local ni = (i % 4) + 1
                    if esp.BoxLines[i] then
                        esp.BoxLines[i].From = pts[i]
                        esp.BoxLines[i].To = pts[ni]
                        esp.BoxLines[i].Color = C3(Settings.ESPColor)
                        esp.BoxLines[i].Thickness = 1.5 * espSizeMult
                        esp.BoxLines[i].Transparency = 1 - espOpacity
                        esp.BoxLines[i].Visible = true
                    end
                end
            elseif Settings.ESPBoxStyle == "Corner" then
                local cornerLen = math.min(w, h) * 0.25
                local pts = {
                    {Vector2.new(cx-w/2, top), Vector2.new(cx-w/2+cornerLen, top)},
                    {Vector2.new(cx-w/2, top), Vector2.new(cx-w/2, top+cornerLen)},
                    {Vector2.new(cx+w/2, top), Vector2.new(cx+w/2-cornerLen, top)},
                    {Vector2.new(cx+w/2, top), Vector2.new(cx+w/2, top+cornerLen)},
                    {Vector2.new(cx-w/2, bot), Vector2.new(cx-w/2+cornerLen, bot)},
                    {Vector2.new(cx-w/2, bot), Vector2.new(cx-w/2, bot-cornerLen)},
                    {Vector2.new(cx+w/2, bot), Vector2.new(cx+w/2-cornerLen, bot)},
                    {Vector2.new(cx+w/2, bot), Vector2.new(cx+w/2, bot-cornerLen)},
                }
                for i = 1, 8 do
                    if esp.CornerLines[i] then
                        esp.CornerLines[i].From = pts[i][1]
                        esp.CornerLines[i].To = pts[i][2]
                        esp.CornerLines[i].Color = C3(Settings.ESPColor)
                        esp.CornerLines[i].Thickness = 2 * espSizeMult
                        esp.CornerLines[i].Transparency = 1 - espOpacity
                        esp.CornerLines[i].Visible = true
                    end
                end
            elseif Settings.ESPBoxStyle == "3D" then
                local ext = Vector3.new(2, 3, 1.5)
                local corners = {
                    root.Position + Vector3.new(-ext.X, ext.Y, -ext.Z),
                    root.Position + Vector3.new(ext.X, ext.Y, -ext.Z),
                    root.Position + Vector3.new(ext.X, ext.Y, ext.Z),
                    root.Position + Vector3.new(-ext.X, ext.Y, ext.Z),
                    root.Position + Vector3.new(-ext.X, -ext.Y, -ext.Z),
                    root.Position + Vector3.new(ext.X, -ext.Y, -ext.Z),
                    root.Position + Vector3.new(ext.X, -ext.Y, ext.Z),
                    root.Position + Vector3.new(-ext.X, -ext.Y, ext.Z),
                }
                local screenP = {}
                local allOn = true
                for i, cor in ipairs(corners) do
                    local sp, on = Camera:WorldToViewportPoint(cor)
                    screenP[i] = Vector2.new(sp.X, sp.Y)
                    if not on then allOn = false end
                end
                if allOn then
                    local edges = {{1,2},{2,3},{3,4},{4,1},{5,6},{6,7},{7,8},{8,5},{1,5},{2,6},{3,7},{4,8}}
                    for i, e in ipairs(edges) do
                        if esp.Box3DLines[i] then
                            esp.Box3DLines[i].From = screenP[e[1]]
                            esp.Box3DLines[i].To = screenP[e[2]]
                            esp.Box3DLines[i].Color = C3(Settings.ESPColor)
                            esp.Box3DLines[i].Thickness = 1.5 * espSizeMult
                            esp.Box3DLines[i].Transparency = 1 - espOpacity
                            esp.Box3DLines[i].Visible = true
                        end
                    end
                end
            end
        end

        for _, l in ipairs(esp.SkeletonLines) do l.Visible = false end
        if Settings.ESPSkeleton then
            for i, bone in ipairs(SkeletonBones) do
                local p1 = ch3:FindFirstChild(bone[1])
                local p2 = ch3:FindFirstChild(bone[2])
                if p1 and p2 and esp.SkeletonLines[i] then
                    local s1, o1 = Camera:WorldToViewportPoint(p1.Position)
                    local s2, o2 = Camera:WorldToViewportPoint(p2.Position)
                    if o1 and o2 then
                        esp.SkeletonLines[i].From = Vector2.new(s1.X, s1.Y)
                        esp.SkeletonLines[i].To = Vector2.new(s2.X, s2.Y)
                        esp.SkeletonLines[i].Color = C3(Settings.SkeletonColor)
                        esp.SkeletonLines[i].Thickness = 1.5 * espSizeMult
                        esp.SkeletonLines[i].Transparency = 1 - espOpacity
                        esp.SkeletonLines[i].Visible = true
                    end
                end
            end
        end

        if Settings.ESPNames and esp.NameText then
            local nameStr = player.Name
            if Settings.ShowPlayerFPS or Settings.ShowPlayerMS then
                if Settings.ShowPlayerFPS then nameStr = nameStr .. " [FPS:"..currentFPS.."]" end
                if Settings.ShowPlayerMS then nameStr = nameStr .. " [MS:"..math.floor(dt*1000).."]" end
            end
            esp.NameText.Text = nameStr
            esp.NameText.Size = 13 * espSizeMult
            esp.NameText.Position = Vector2.new(cx, top - 18)
            esp.NameText.Color = C3(Settings.ESPColor)
            esp.NameText.Transparency = 1 - espOpacity
            esp.NameText.Visible = true
        elseif esp.NameText then esp.NameText.Visible = false end

        if Settings.ESPDistance and esp.DistText and LocalPlayer.Character then
            local lp = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if lp then
                local d = math.floor((lp.Position - root.Position).Magnitude)
                esp.DistText.Text = "["..d.."m]"
                esp.DistText.Size = 11 * espSizeMult
                esp.DistText.Position = Vector2.new(cx, bot + 4)
                esp.DistText.Transparency = 1 - espOpacity
                esp.DistText.Visible = true
            end
        elseif esp.DistText then esp.DistText.Visible = false end

        if Settings.ESPWeapon and esp.WeaponText then
            local tool = ch3:FindFirstChildOfClass("Tool")
            if tool then
                esp.WeaponText.Text = "[" .. tool.Name .. "]"
                esp.WeaponText.Size = 11 * espSizeMult
                esp.WeaponText.Position = Vector2.new(cx, bot + 18)
                esp.WeaponText.Color = Color3.fromRGB(255, 200, 100)
                esp.WeaponText.Transparency = 1 - espOpacity
                esp.WeaponText.Visible = true
            else esp.WeaponText.Visible = false end
        elseif esp.WeaponText then esp.WeaponText.Visible = false end

        if Settings.ESPHealth and esp.HpBG and esp.HpFill then
            local hp = math.clamp(hu.Health / hu.MaxHealth, 0, 1)
            local bx = cx - w/2 - 7
            esp.HpBG.From = Vector2.new(bx, top)
            esp.HpBG.To = Vector2.new(bx, bot)
            esp.HpBG.Transparency = 1 - espOpacity
            esp.HpBG.Visible = true
            esp.HpFill.Color = Color3.fromRGB(math.floor((1-hp)*220), math.floor(hp*220), 30)
            esp.HpFill.From = Vector2.new(bx, bot)
            esp.HpFill.To = Vector2.new(bx, bot - h * hp)
            esp.HpFill.Thickness = 3 * espSizeMult
            esp.HpFill.Transparency = 1 - espOpacity
            esp.HpFill.Visible = true
        else
            if esp.HpBG then esp.HpBG.Visible = false end
            if esp.HpFill then esp.HpFill.Visible = false end
        end

        if Settings.ESPTracers and esp.Tracer then
            esp.Tracer.Color = C3(Settings.ESPColor)
            esp.Tracer.From = Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y)
            esp.Tracer.To = Vector2.new(cx, bot)
            esp.Tracer.Thickness = 1.5 * espSizeMult
            esp.Tracer.Transparency = 1 - espOpacity
            esp.Tracer.Visible = true
        elseif esp.Tracer then esp.Tracer.Visible = false end

        if Settings.ESPSnapLines and esp.SnapLine then
            esp.SnapLine.Color = C3(Settings.ESPColor)
            esp.SnapLine.From = center
            esp.SnapLine.To = Vector2.new(cx, (top + bot) / 2)
            esp.SnapLine.Transparency = 1 - espOpacity
            esp.SnapLine.Visible = true
        elseif esp.SnapLine then esp.SnapLine.Visible = false end

        if esp.DormantMarker then esp.DormantMarker.Visible = false end
    end
end)

-- ========== DRAG ==========
local guiOpen = false
local mDr, mDs, mSp = false
TitleBar.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        mDr = true; mDs = i.Position; mSp = MainFrame.Position
    end
end)
UserInputService.InputChanged:Connect(function(i)
    if mDr and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        local d = i.Position - mDs
        MainFrame.Position = UDim2.new(mSp.X.Scale, mSp.X.Offset + d.X, mSp.Y.Scale, mSp.Y.Offset + d.Y)
    end
end)
UserInputService.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then mDr = false end
end)

local tDr, tDs, tSp, tMv = false, nil, nil, false
ToggleButton.InputBegan:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
        tDr = true; tMv = false; tDs = i.Position; tSp = ToggleButton.Position
    end
end)
UserInputService.InputChanged:Connect(function(i)
    if tDr and not Settings.ToggleButtonLocked and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
        local d = i.Position - tDs
        if d.Magnitude > 7 then
            tMv = true
            ToggleButton.Position = UDim2.new(tSp.X.Scale, tSp.X.Offset + d.X, tSp.Y.Scale, tSp.Y.Offset + d.Y)
        end
    end
end)
UserInputService.InputEnded:Connect(function(i)
    if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then tDr = false end
end)

ToggleButton.MouseButton1Click:Connect(function()
    if tMv then tMv = false; return end
    guiOpen = not guiOpen
    MainFrame.Visible = guiOpen
    if guiOpen then
        MainFrame.BackgroundTransparency = 1
        MainFrame.Size = UDim2.new(0, BASE_WIDTH * 0.85, 0, BASE_HEIGHT * 0.85)
        TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            BackgroundTransparency = 0,
            Size = UDim2.new(0, BASE_WIDTH * (Settings.GUIScale/100), 0, BASE_HEIGHT * (Settings.GUIScale/100))
        }):Play()
    end
end)

CloseButton.MouseButton1Click:Connect(function()
    guiOpen = false
    MainFrame.Visible = false
    if ActivePicker then ActivePicker:Destroy(); ActivePicker = nil end
end)

MinButton.MouseButton1Click:Connect(function()
    minimized = not minimized
    if minimized then
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart), {
            Size = UDim2.new(0, BASE_WIDTH * (Settings.GUIScale/100), 0, 52)
        }):Play()
        ContentArea.Visible = false
        TabBar.Visible = false
    else
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart), {
            Size = UDim2.new(0, BASE_WIDTH * (Settings.GUIScale/100), 0, BASE_HEIGHT * (Settings.GUIScale/100))
        }):Play()
        task.wait(0.3)
        ContentArea.Visible = true
        TabBar.Visible = true
    end
end)

local wallJumpDebounce = false
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.Space and Settings.WallJump and not wallJumpDebounce then
        local ch3 = LocalPlayer.Character
        if not ch3 then return end
        local hrp = ch3:FindFirstChild("HumanoidRootPart")
        local hu = ch3:FindFirstChildOfClass("Humanoid")
        if not (hrp and hu) then return end
        if hu:GetState() == Enum.HumanoidStateType.Freefall then
            local ray = Ray.new(hrp.Position, hrp.CFrame.LookVector * 3)
            local hit = workspace:FindPartOnRayWithIgnoreList(ray, {ch3})
            if hit then
                wallJumpDebounce = true
                hrp.Velocity = Vector3.new(hrp.Velocity.X, 60, hrp.Velocity.Z) - (hrp.CFrame.LookVector * 30)
                task.wait(0.3)
                wallJumpDebounce = false
            end
        end
    end
end)

UserInputService.JumpRequest:Connect(function()
    if Settings.InfinityJump then
        local ch3 = LocalPlayer.Character
        if ch3 then local hu = ch3:FindFirstChildOfClass("Humanoid"); if hu then hu:ChangeState(Enum.HumanoidStateType.Jumping) end end
    end
end)

print("✦ AIMBOT PRO v13.2 - SAFE EDITION ✦")
print("✦ GUI Scale | Loading Screen | No Particles ✦")